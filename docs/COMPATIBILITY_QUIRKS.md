# 互換性上の注意事項

この文書は、なでしこ3 v3.7.24（`aa18c7e`）の命令説明だけからは判断しにくい挙動を記録します。
lnakoは、意図的な非互換として合意した項目を除き、説明文ではなく固定した公式実装と差分テストの結果を
互換基準にします。項目を追加するときは、公式実装の場所、lnako側の扱い、差分テストIDを併記します。

## ソース正規化

| 境界 | 公式v3.7.24の実際の挙動 | lnakoの扱い | 差分テストID |
|---|---|---|---|
| 文字列内のCRLF / CR | ソース前処理はコード部分だけでなく、`『…』`などの文字列内もCRLFとCRをLFへ1文字で正規化する | 公式と同じく読込時にLFへ正規化する。実行時の文字列値としてCRLFを渡すテストは`CHR(13)&CHR(10)`で生成し、ソース改行の正規化と混同しない | `公式前処理の全角記号と改行規則に合わせる`、`native-system-json-decode-errors` |

## JSON（plugin_system）

公式実装は `.cache/oracle/nadesiko3-3.7.24/core/src/plugin_system_json.mts` の
`JSON.stringify` / `JSON.parse` 呼び出しです。以下の境界はインタープリタとAOTの差分fixtureで固定しています。
JSONエンコード5命令（`JSON変換`、`JSONエンコード`、`JSON_E`、`JSONエンコード整形`、`JSON_ES`）は
純LLVM AOTへ接続し、native fixtureで公式CLI・公式生成JavaScript・`lnako run`・AOT O0〜O3を比較します。
JSONデコード3命令（`JSON取得`、`JSONデコード`、`JSON_D`）は、インタープリタと純LLVM AOTの両方で
UTF-16コード単位を直接解析する明示スタックパーサーへ接続します。同じnative fixtureで3別名と
サロゲート・数値・重複キー・主要エラーを上記7経路で比較します。

| 境界 | 公式v3.7.24の実際の挙動 | lnakoの扱い | 差分テストID |
|---|---|---|---|
| 辞書キーの正規化・列挙順 | JavaScriptのproperty keyとして数値`1`と文字列`"1"`は同じキーになり、後の代入が値を上書きしても最初の挿入位置は動かない。canonical array index（`0`〜`4294967294`の十進表記）は数値昇順、その後の文字列キーは挿入順で`JSON.stringify`する。`01`はindexではない | JSON境界と辞書7命令の列挙ではproperty keyを文字列へ正規化し、重複を解消して同じ順序へ並べ替える。AOTの辞書代入も文字列キーへ正規化する。関数値をキーにした場合の関数名を含む文字列化は`TODO: aot-function-string-name`を維持する | `plugin-system-json-ecmascript-boundaries`、`native-system-json-encode`、`native-system-dictionary-hash-commands`、`JSONの辞書キーをECMAScriptの列挙順で整形する`、`TODO: aot-function-string-name` |
| トップレベルの`undefined` / 関数値 | `JSON.stringify(undefined)` と `JSON.stringify(function(){})` は値を返さず`undefined`になる。配列要素ではどちらも`null`になる | `encode`のトップレベル判定は`undefined`を返し、配列内の値は`null`へ変換する | `plugin-system-json-ecmascript-boundaries` |
| BigInt / 循環参照 | `JSON.stringify(1n)` は `Do not know how to serialize a BigInt`、循環参照は開始constructor・中間のproperty/index経路・閉路位置を含む実行時エラー | インタープリタとAOTは`CannotSerializeBigInt` / `CircularCloneValue`へ正規化し、V8と同じactive container経路をエラー文言へ反映する | `plugin-system-json-ecmascript-boundaries`、`native-system-json-encode`、`JSONの実行時エラー文言を公式互換にする` |
| 不正JSON | `JSON.parse("x")` は `Unexpected token 'x', "x" is not valid JSON`。`undefined`・`Infinity`は全体を引用したエラーになり、先頭0、配列・辞書の区切り不足、文字列制御文字・Unicode escapeにも位置付きV8文言がある。長い入力は不正位置の前後10 UTF-16コード単位を`...`で省略し、診断用sourceの引用符・バックスラッシュ・制御文字はエスケープしない | インタープリタとAOTはUTF-16入力を直接解析し、Node 24の文言・position/line/column・CR/CRLF境界と、20コード単位以下の全文表示、長文の前後窓表示を保持する。`failureMessageValue` / AOT例外Valueは孤立サロゲートも保持し、UTF-8標準出力時だけU+FFFDへ置換する | `plugin-system-json-utf16-boundaries`、`JSONデコードのNode 24エラー位置を保持する`、`AOT JSONエラー文言は孤立サロゲートをUTF-16で保持する`、`native-system-json-decode-errors` |
| 孤立サロゲート | `JSON.parse("\"\\ud800\"")` とlow surrogateは受理し、UTF-16コード単位を保持する。`JSON.parse`の例外messageにも元の孤立コード単位が残る | インタープリタとAOTはhigh/low surrogateとペアを保持し、エラー監視の`エラーメッセージ`とAOT例外ValueもUTF-16のまま保持する。UTF-8標準出力の境界だけで孤立サロゲートをU+FFFDへ置換する | `plugin-system-json-utf16-boundaries`、`JSONエラー文言は孤立サロゲートをUTF-16で保持する`、`AOT JSONエラー文言は孤立サロゲートをUTF-16で保持する`、`native-system-json-decode`、`native-system-json-decode-errors` |
| JSONパース深度 | Nodeの受理深度は実装・スタック条件に依存し、固定の仕様値ではない | インタープリタとAOTはCスタックに再帰せず、入力中のコンテナ数に比例する明示スタックを使う。配列と辞書の100,000段を単体テストし、固定の1024段制限は持たない | `JSONデコードは100000段のネストをCスタックなしで処理する`、`AOT JSONデコードは深い配列と辞書を明示スタックで処理する` |

## 配列・表・辞書

| 命令 | 公式v3.7.24の実際の挙動 | lnakoの扱い | 差分テストID |
|---|---|---|---|
| `辞書キー列挙` / `ハッシュキー列挙` / `ハッシュ内容列挙` / `辞書キー削除` / `ハッシュキー削除` / `辞書キー存在` / `ハッシュキー存在` | 実装は`for...in` / `key in` / `delete`に基づく。辞書のcanonical array indexは数値昇順、その他のown文字列キーは挿入順。配列キーは文字列の`"0"`形式、非index own文字列キーも挿入順、`length`は存在し、内容列挙は配列要素とown property値を返す。`key in`は辞書・配列・関数・Buffer/Uint8Array/ArrayBufferの標準prototype chainも調べ、Buffer/Uint8Arrayのcanonical indexと`length`はown propertyとして存在する。関数のキー列挙・内容列挙は空だが`length`/`name`と標準prototype propertyは存在する。配列のindex削除はholeになり、`length`削除は`Cannot delete property 'length' of [object Array]`になる | Interpreter/AOTで7命令、辞書のnumeric key順、配列キーの文字列化と非index own propertyの挿入順列挙、配列内容列挙、関数の空列挙と`length`/`name`存在を実装する。`辞書キー存在`/`ハッシュキー存在`はown propertyを優先し、辞書・配列・関数について標準prototypeの`constructor`・代表的method・`__proto__`・関数の`prototype`を、Buffer/Uint8Array/ArrayBufferについてbyte length系scalar・代表的method・標準prototypeを存在判定へ含める。Buffer/Uint8Arrayのcanonical indexと`length`、ArrayBufferのindex非存在もInterpreter/AOTで同じく扱う。AOTの動的property keyはToPropertyKeyへ正規化する。`native-system-array-sparse-presence`と`native-system-array-sparse-copy-reference-concat`で配列の伸長中間slot・index削除・slice/concat結果のpresenceを追跡し、キー・内容列挙・存在判定を公式と一致させる。表命令等の他の疎動作は`TODO: sparse-array-presence`として分離する。バイト列のown index列挙・削除は`TODO: dictionary-byte-buffer-enumeration`として完成扱いにしない | `native-system-dictionary-hash-commands`、`native-system-dictionary-inherited-properties`、`native-system-dictionary-byte-buffer-properties`、`native-system-array-own-properties`、`native-system-array-sparse-presence`、`native-system-array-sparse-copy-reference-concat`、`plugin-system-dictionary-hash`、`TODO: sparse-array-presence`、`TODO: dictionary-byte-buffer-enumeration` |
| 配列命令の助詞省略 | C風呼び出しの引数不足は文法エラーだが、助詞構文では`[1,2]を配列結合`の区切り値と`[1,2]から配列検索`の検索値が`undefined`として渡され、前者は`1undefined2`、後者は`-1`になる | AOTは1引数の静的呼び出しへ`undefined`を補い、C風呼び出しの引数数診断は共通の`TODO: builtin-arity-diagnostics`として分離する | `native-system-array-particle-omission`、`TODO: builtin-arity-diagnostics` |
| `配列結合`・`配列只結合` | 配列では`Array.join`相当で、`null`・`undefined`・疎配列の要素を空文字列として扱い、入れ子配列はカンマ区切りの文字列化、循環参照は空文字列になる。区切り文字は`''+S`で文字列化される。配列以外は`String(A).split('\\n').join(S)`となるため、改行前後の空要素も区切り文字で結ばれる。`配列只結合`の区切り文字は空文字列 | AOTもUTF-16コード単位の値文字列化、nullish・疎配列・循環配列・非配列の改行分割を共有する。辞書のカスタム`valueOf` / `toString`を伴うDefault ToPrimitiveの完全互換は`TODO: aot-object-to-primitive`、関数は生成wrapper名を含む文字列へ変換するが公式関数ソース本文まで含める完全互換は`TODO: aot-function-string-name`として完成扱いにしない | `native-system-array-join-search-commands`、`plugin-system-array-core` |
| `配列検索` | 配列に対する`Array.indexOf`相当で、0始まりの最初の一致か`-1`を返す。比較はstrict equalityなのでNaNは一致せず、±0は一致し、数値と文字列、`null`と`undefined`、同内容の別オブジェクトは一致しないが同じ参照は一致する。疎配列のholeはスキップされるため、明示的な`undefined`とは一致しない。配列以外は常に`-1` | AOT・インタプリタとも既存のstrict equalityと配列参照同一性を使い、`native-system-array-sparse-presence`で存在しないslotを比較対象からスキップする。holeと明示的`undefined`の`indexOf`差分を7経路で確認済み | `native-system-array-join-search-commands`、`native-system-array-particle-omission`、`native-system-array-sparse-presence`、`plugin-system-array-core` |
| `配列ソート` | `Array.sort()`相当の破壊的な安定ソートで、比較値はToString後のUTF-16コード単位順。明示的`undefined`とholeは比較せず末尾へ移動し、明示的`undefined`がholeより前、同値の順序は維持する | AOT・インタプリタともbottom-up stable merge sortでO(n log n)を確保し、値とpresence bitmapを同時に並べ替えて同一配列を返す。AOT関数値の公式ソース本文までの文字列化は`TODO: aot-function-string-name`を維持する | `native-system-array-sort-commands`、`native-system-array-sparse-ordering`、`plugin-system-array-core`、`TODO: aot-function-string-name` |
| `配列数値ソート` | 比較関数`(a,b)=>parseFloat(a)-parseFloat(b)`による破壊的な安定ソート。`undefined`・holeは末尾（明示的`undefined`がholeより前）、NaN comparator結果は0扱い、±0は同値。BigIntも`parseFloat`で数値化し、元要素自体は変換しない | AOT・インタプリタともparseFloat接頭辞規則、NaN同値、±0同値、BigInt、undefined/holeの順序をpresence bitmapとともに保持する。同一配列を返す | `native-system-array-sort-commands`、`native-system-array-sparse-ordering`、`plugin-system-array-core` |
| `表ソート` / `表数値ソート` | 公式は行配列の指定列（0起点またはproperty key）を読み、前者は抽象関係比較、後者は`ns-ms`のNumber変換で`Array.sort`を破壊的に安定ソートする。戻り値は元配列。最上位配列の要素が明示的`undefined`なら定義値の後、holeなら明示的`undefined`の後へ移動し、比較関数は呼ばれない。null行、行内部の疎property、BigInt・NaN comparatorの境界はJavaScriptの型・ソート実装に依存する | InterpreterとAOTは行propertyと元配列identityを保持し、前者を抽象関係比較、後者をNumber変換へ接続する。`native-system-table-sparse-sort`で最上位配列の値・明示的`undefined`・holeを値とpresence bitmapごと安定ソートする。行内部の疎property、複雑な継承property、BigIntの返却変換は未実装（`TODO: table-sort-js-boundaries`）。QuickJS互換モードは対象外 | `plugin-system-table`、`native-system-table-sort`、`native-system-table-sparse-sort`、`TODO: table-sort-js-boundaries` |
| `配列数値変換` / `配列逆順` | 前者は各要素へ`parseFloat`結果を書き込む破壊的操作（holeもundefined読込からNaNを書込）、後者は`reverse()`相当の破壊的操作。非配列エラー時の公式`配列逆順`文言は`配列ソート`と誤記される | 数値変換はholeを明示的な`NaN`へmaterializeし、逆順は値とpresence bitmapを反転する。いずれも同一配列を返す。公式カタログの4命令は`pure:true`だが実際には破壊的なので、AOTのDCE・共通部分式削除の根拠にしない | `native-system-array-sort-commands`、`native-system-array-sparse-ordering`、`plugin-system-array-core`、`TODO: builtin-pure-side-effect-metadata` |
| `配列シャッフル` | `Math.random()`を使うFisher–Yates方式で配列を破壊的に並べ替え、元配列を返す。各回の`a[i]=a[r]`・`a[r]=tmp`は読み出したholeを`undefined`として代入先propertyを実在化する。乱数列は実行ごとに変わるため、並び順そのものは固定契約ではない | Interpreterと純LLVM AOTで同じ方式・同一配列identity・要素置換を保ち、各swapの2つの代入先をpresentにする。テスト用乱数源は差し替え可能だが、公式乱数列との一致を意味しない | `plugin-system-array-core`、`native-system-array-shuffle`、`native-system-array-sparse-ordering` |
| `配列カスタムソート` / `配列関数適用` / `配列マップ` / `配列フィルタ` | コールバックは関数値または文字列で指定でき、文字列は`sys.__findFunc`で解決する。カスタムソートは元配列を破壊して同じ配列を返し、関数適用・マップ・フィルタは各要素をコールバックへ渡して新配列を返す。フィルタはtruthyのとき元の要素を保持する | Interpreterと純LLVM AOTで関数値・モジュール修飾名の一意な短縮名解決、安定ソート、同一配列／新配列のidentityを実装する。AOTはモジュール初期化時に名前付き関数をGCルートへ登録する。QuickJS互換モードはこの標準命令の証拠経路に含めない。比較関数のV8固有の呼出順と副作用順序は`TODO: custom-sort-callback-order`として未実装境界に分離する | `plugin-system-array-callbacks`、`native-system-array-callbacks`、`TODO: custom-sort-callback-order` |
| `配列カスタムソート`の副作用付き比較関数 | 公式はNode 24の`Array.sort`へ比較関数を渡すため、比較関数の呼出順・回数はV8のソート実装に依存する。比較中に元配列を短縮しても、比較開始前に収集した要素を元の長さまで書き戻す | lnakoも比較前に要素を収集し、元配列の短縮・再確保で範囲外参照しない。純粋な比較関数の安定ソート結果は差分検証済みだが、比較関数が外部状態を更新する場合のV8固有の呼出順は`TODO: custom-sort-callback-order`として完成扱いにしない | `plugin-system-array-callbacks`、`カスタムソート中に元配列が短縮されても収集済み要素を安全に書き戻す`、`TODO: custom-sort-callback-order` |
| `配列要素数`・`要素数`・`LEN` | 文字列はUTF-16コード単位数を返すため`LEN("A😀B")`は4になる。配列は要素数、辞書は列挙可能なキー数、関数とPromiseは`Object.keys`相当の0、その他のプリミティブは1になる | インタープリタとAOTで値タグごとの同じ規則を使い、Unicode scalar数を返す`文字数`と分離する | `関数とPromiseの要素数はObject.keysと同じ0にする`、`AOT文字長検索と要素数はUnicode scalarとUTF-16を区別する`、`native-system-string-length-search-boundary-commands` |
| `配列挿入` | 元配列へ要素を挿入するが、戻り値は変更後配列ではなく、削除要素0件を表す空配列。内部で `splice(i, 0, s)` の戻り値をそのまま返す。`i`は`splice`のToIntegerOrInfinity（文字列はNumber変換、負数は末尾基準、小数は0方向切捨て、NaNは0）。既存のholeは後ろへ移動し、挿入要素の添字は実在する | 同じく元配列を変更し、値とpresence bitmapをspliceする。holeを含む配列での挿入と空配列の戻り値を保持する | `plugin-system-array-mutation-quirks`、`native-system-array-mutation-commands`、`native-system-array-sparse-splice` |
| `配列一括挿入` | 元配列を変更し、こちらは変更後の元配列を返す。公式実装は各回の`i+j`を評価するため、`i`が文字列なら加算ではなく連結になる。空でない配列で`a===b`の場合は`b.length`も増えるため、公式ループが終了しない（空配列なら0回で終了）。挿入元のholeを読み出した値は`undefined`として挿入先propertyを実在化する | 通常の値は公式どおり各回の`i+j`を評価し、挿入された添字をpresentにする。同一配列は先に挿入元をコピーして有限化する安全差があり、公式との差分として固定する（`TODO: array-bulk-self-alias-official-loop`） | `plugin-system-array-mutation-quirks`、`native-system-array-mutation-commands`、`native-system-array-sparse-splice`、`TODO: array-bulk-self-alias-official-loop` |
| `配列削除` | `配列切取`の別名として動き、配列では`i`がNumber型のときだけ単一要素を削除して返す。文字列数値、BigInt、真偽値、undefinedは単一削除に入らず`null`になる（nullは`i['先頭']`参照でTypeError）。削除対象がholeなら戻り値は`undefined`だが、後続の実在propertyは前へ移動する | 同じ型分岐と`splice`数値化をAOT・インタプリタで再現し、削除元のpresence bitmapを後続要素とともに詰める | `plugin-system-array-mutation-quirks`、`native-system-array-mutation-commands`、`native-system-array-sparse-splice` |
| `配列切取`（辞書） | キーの値がtruthyの場合だけ削除する。値が`0`、空文字列、`false`などの場合はキーを残して`undefined`を返す。公式の継承プロパティ（`toString`、`__proto__`）の参照はownキー削除と両立しない | ownキーの挿入順とtruthy判定を再現する。継承プロパティを持つJavaScript Objectとの完全互換は`TODO: array-cut-inherited-dictionary-properties`として完成扱いにしない | `plugin-system-array-mutation-quirks`、`native-system-array-mutation-commands`、`TODO: array-cut-inherited-dictionary-properties` |
| `配列取出` | `splice(i,cnt)`の結果配列を返す。`i`と`cnt`はToIntegerOrInfinityで数値化し、負数・NaN・Infinity・小数・文字列をJavaScript規則で処理する。切り出した配列のholeはholeのまま、元配列の後続propertyは前へ移動する | 同じ。BigIntをNumberへ暗黙変換せず公式と同じ実行時エラーにし、削除元と戻り値のpresence bitmapを別々に移動する | `plugin-system-array-mutation-quirks`、`native-system-array-mutation-commands`、`native-system-array-sparse-splice` |
| `配列ポップ` | `Array.pop()`相当で末尾要素を返し、空配列では`undefined`を返す。元配列は変更される | 同じ。戻り値を配列長と取り違えない | `plugin-system-array-mutation-quirks`、`native-system-array-mutation-commands` |
| `配列追加` / `配列プッシュ` | 元配列を変更し、JavaScriptの`push`が返す要素数ではなく、変更後配列を返す | 同じ | `plugin-system-array-mutation-quirks`、`native-system-array-mutation-commands` |
| `配列最大値` / `配列最小値` | `reduce`と`Math.max`/`Math.min`の組合せなので、空配列は例外、2要素以上では数値変換失敗がNaNを伝播し、BigIntの暗黙数値化も例外になる。単一要素だけは変換せず文字列・BigInt・オブジェクト自身を返す。±0の符号も`Math.max`/`Math.min`規則に従い、疎配列のholeは`reduce`が飛ばす | インタープリタとAOTは単一要素の型とidentityを保ち、複数要素ではBigInt変換をエラーにしてNaN伝播と±0を保持する。`native-system-array-sparse-presence`で先頭要素の探索と後続のholeスキップを実装し、公式差分を確認済み。全slotがholeの場合の公式診断文言など、未収録の境界は別途検証する | `plugin-system-array-core`、`native-system-array-aggregate-create-commands`、`native-system-array-sparse-presence`、Zig単体テスト |
| `配列合計` | 要素ごとに`parseFloat`し、NaNだけを飛ばして加算する。文字列の接頭数値とBigIntは加算できるが、配列以外は例外 | AOTは同じ`parseFloat`接頭辞規則とNaN除外を使う | `plugin-system-array-core`、`native-system-array-aggregate-create-commands` |
| `配列入替` | `a[i]`と`a[j]`をJavaScriptのプロパティキーとして読み書きするため、整数添字の範囲外は疎配列を伸長し、それ以外の文字列・小数・NaN・Infinity・BigIntキーは配列要素ではないown propertyになる | lnakoはToPropertyKeyで正規化した非indexキーをown propertyとして挿入順に保持し、数値の非負整数添字と範囲外伸長も実装する。密配列表現で巨大添字がメモリを枯渇させないよう、伸長後100万要素超は確保前にエラーにする。疎配列差は`TODO: array-swap-dense-length-limit`として完成扱いにしない | `plugin-system-array-core`、`native-system-array-own-properties`、`native-system-array-aggregate-create-commands`、Zig単体テスト、`TODO: array-swap-dense-length-limit` |
| `配列連番作成` | `for(let i=A;i<=B;i++)`をそのまま実行するため、開始値が文字列・null・真偽値なら最初の要素だけ元の型を保ち、その後はNumberになる。BigIntはBigIntのまま増加する。InfinityやNumber精度上限付近では停止しない場合がある | インタープリタとAOTは比較、開始値の型保持、BigInt加算を実装する。停止しない列はプロセスを枯渇させないよう最大100万要素でエラーにするため、通常の差分CIでは直接実行しない | `plugin-system-array-core`、`native-system-array-aggregate-create-commands`、`TODO: array-sequence-unbounded-loop-limit` |
| `配列要素作成` | 数値shapeでは各要素を深く複製し、shape配列では再帰的な多次元配列を作る。`shape=[]`は空配列、undefinedや関数は同一値を保ち、配列要素のundefinedも保持する。Infinity長はRangeError | インタープリタとAOTは多次元生成、空shape、GCルート保護、未定義・関数・配列の複製を実装する。各次元の積と全階層の要素総数を確保前に検査し、Infinityまたは合計100万超は即時エラーにする。辞書のホスト固有オブジェクト複製境界は`TODO: array-fill-dictionary-json-clone`、安全上限差は`TODO: array-fill-total-size-limit`として分離する | `plugin-system-array-core`、`native-system-array-aggregate-create-commands`、Zig単体テスト、`TODO: array-fill-dictionary-json-clone`、`TODO: array-fill-total-size-limit` |
| 配列変更命令の`pure:true`属性 | 公式カタログは`配列挿入`、`配列一括挿入`、`配列削除`、`配列切取`、`配列取出`、`配列ポップ`、`配列プッシュ`、`配列追加`、`配列入替`を`pure:true`として登録しているが、実装は元配列・辞書を破壊的に変更する | AOTは元コンテナへの書き込みを保持し、これらの命令を純粋式としてDCE・共通部分式削除する最適化根拠には使わない。カタログ属性と実行時副作用の統一は`TODO: builtin-pure-side-effect-metadata`として分離する | `plugin-system-array-mutation-quirks`、`native-system-array-mutation-commands`、`native-system-array-aggregate-create-commands`、`TODO: builtin-pure-side-effect-metadata` |
| `配列複製` | JSONの文字列化・再解析による深い複製。循環参照とBigIntは失敗し、単体の`undefined`・関数もJSON文字列が得られず失敗する。配列要素の`undefined`・関数はnull、辞書値ならキーごと省略される | AOTもJSON規則（有限でない数値はnull、配列要素のundefined/functionはnull、辞書要素は省略、循環とBigIntはエラー）を保持する | `plugin-system-array-core`、`native-system-array-copy-reference-commands`、`native-system-array-clone-undefined-error`、`native-system-array-clone-bigint-error`、`native-system-array-clone-circular-error` |
| 配列コピー・参照命令の未捕捉実行時例外 | 公式CLI直接実行は`配列複製`のundefined・BigInt・循環参照、`配列範囲コピー`のnull範囲・非配列、`参照`のnull受信値・不正文字列範囲を標準出力へ表示して終了コード0にする。一方、公式生成JavaScript＋Nodeは未捕捉例外として終了コード1になる | `lnako run`とAOTはエラーを成功扱いにせず、公式生成JavaScript側に合わせて終了コード1にする。差分fixtureは`oracle: official-generated`を明示し、公式CLIとの差を隠さない | `native-system-array-clone-undefined-error`、`native-system-array-clone-bigint-error`、`native-system-array-clone-circular-error`、`native-system-array-range-copy-null-error`、`native-system-array-range-copy-receiver-error`、`native-system-reference-null-error`、`native-system-reference-string-range-error` |
| `配列範囲コピー` | 数値指定は要素を返し、要素がオブジェクトならJSON複製する。BigInt単一指定は`typeof i === 'number'`に入らず`undefined`になる。`先頭`・`末尾`範囲は`slice`した配列全体をJSON複製するため、`undefined`はnullになり、参照とは異なってネストしたコンテナも独立する。範囲の`末尾`は明示的な`Number`変換なのでBigIntも丸めて扱うが、`先頭`はNumber型だけが範囲指定として認識される | AOT・インタープリタは範囲終端だけBigInt.toF64相当へ変換し、負数・小数・範囲外をJavaScriptの`slice`規則で処理する。範囲結果を深く複製する。配列切取の暗黙減算経路は別規則として変更しない | `plugin-system-array-core`、`native-system-array-copy-reference-commands`、`native-system-array-range-copy-null-error`、`native-system-array-range-copy-receiver-error`、`native-system-array-range-copy-bigint-coercion`、Zig単体テスト |
| `参照` / `配列参照` | 文字列・配列の数値指定は要素、範囲指定は`substring`/`slice`の新しいコンテナを返す。文字列範囲だけは始点と終点が逆なら交換する。辞書指定は値をそのまま返し、配列・辞書要素は深く複製しない。不正な文字列範囲は`JSON.stringify`相当の値を`『参照』で文字列型の範囲指定(...)が不正です。`へ埋め込み、undefined・関数は`undefined`、BigInt・循環参照はJSON変換自体の例外を優先する。文字列・配列の範囲末尾は明示的な`Number`変換でBigIntを受理し、配列の単一指定はToPropertyKeyによるown property lookupとして扱う。配列の文字列プロパティはcanonicalな`"0"`・`"1"`などの添字と`"length"`を参照できる（`"01"`、`"-0"`、小数表記、空文字列、`4294967295`以上は添字にならない）。`配列参照`は公式でも`参照`へ委譲する別名であり、同じ結果になる。破壊的な`配列切取`との比較調査では、先の呼び出しによる配列変更を別名差と誤認しないよう入力を作り直す | AOT・インタープリタは文字列のUTF-16コード単位、配列要素の参照、孤立サロゲートを含むエラー文言を保持し、明示的な範囲終端だけBigInt.toF64相当へ変換する。配列・辞書の標準prototype propertyはown値を優先して参照し、未登録なら`undefined`にする。custom prototype・descriptor・receiver・identity境界は`TODO: reference-inherited-properties`として分離する | `plugin-system-array-core`、`native-system-array-copy-reference-commands`、`native-system-reference-array-string-properties`、`native-system-reference-inherited-properties`、`native-system-array-own-properties`、`native-system-reference-null-error`、`native-system-reference-string-range-error`、`native-system-reference-string-range-error-details`、`native-system-array-range-copy-bigint-coercion`、Zig単体テスト、`TODO: reference-inherited-properties` |
| `配列足` | 第1引数が配列なら`concat`相当の新配列を返す（第2引数が配列なら要素を展開）。配列でなければ第2引数を使わず、第1引数だけをJSON複製する | AOTも元配列を変更せず、配列要素は浅く連結し、非配列は`配列複製`と同じJSON規則で処理する | `plugin-system-array-core`、`native-system-array-copy-reference-commands` |
| `表ピックアップ` | 各行へ`row[col]`を行い、その値を`String`へ変換して部分文字列を検索する。配列・辞書・文字列・関数・Buffer・Uint8Array行も扱うが、null/undefined行ではプロパティ参照時に例外になる。ArrayBuffer自身には`length`と数値添字がない。公式生成のなでしこ関数は可変引数ラッパーなので、言語上の引数数によらず`length`は`0`、匿名関数の`name`は空文字列 | AOT・インタープリタで配列添字、配列の非index own property、辞書ownキー、文字列のUTF-16添字、関数の`name`/`length`と`String`変換を実装する。さらにown propertyを優先してObject/Array/String/Functionと数値・真偽値・BigIntの標準constructor・代表的prototype methodを読み出し、`__proto__`とユーザー関数のprototype objectもfixture対象にする。AOT・インタプリタともBuffer/Uint8Arrayの`length`・数値添字を読み、ArrayBufferは`length`・添字なしとして扱う。標準methodは名前を持つ合成関数値であり、receiver付き呼出し、descriptor・identity・prototype変更までは実装しない。byte bufferのraw receiver methodとsparse配列は各TODOとして分離する | `plugin-system-table`、`native-system-table-query-commands`、`native-system-table-byte-row-properties`、`native-system-table-function-properties`、`native-system-table-inherited-properties`、`native-system-array-own-properties`、`TODO: aot-byte-buffer-value`、`TODO: table-inherited-properties`、`TODO: sparse-array-presence` |
| `表完全一致ピックアップ` | `row[col] === value`のstrict equalityで行を選び、行オブジェクト自身を結果配列へ保持する。数値・文字列・BigIntは相互変換しない | 同じstrict equalityと行identityをAOT・インタプリタで保持し、配列の非index own property、Buffer/Uint8Arrayの数値添字とArrayBufferの欠損添字も公式どおり扱う。関数の`name`/`length`に加え、own propertyを優先した標準prototype propertyの読み出しを実装する。合成methodのreceiver付き呼出し、descriptor・identity・prototype変更、残るbyte buffer propertyとsparse配列は別TODOで追跡する | `plugin-system-table`、`native-system-table-query-commands`、`native-system-table-byte-row-properties`、`native-system-table-function-properties`、`native-system-table-inherited-properties`、`native-system-array-own-properties`、`TODO: table-inherited-properties`、`TODO: sparse-array-presence` |
| `表検索` | `for (let i = row; i < a.length; i++)`のrawな開始値を使う。比較が成立した負数・小数・null等は、存在しない`a[i]`から列を読む時点で例外になる。NaN・undefined・正のInfinityや通常の辞書は比較不成立で`-1`。開始値が文字列なら最初の一致だけ文字列を返して以後はNumber、BigIntなら加算後もBigInt、配列等も最初の一致では元の型を返す | AOT・インタプリタともrelational comparison、ToPropertyKey、Number/BigIntの後置インクリメント、rawな戻り値型を共有し、Buffer/Uint8Arrayの数値添字も読む。行・列ではown propertyを優先して標準prototype propertyの読み出しを行う。receiver付きmethod呼出しなど完全な継承prototype semanticsは`TODO: table-inherited-properties`として分離する | `plugin-system-table`、`native-system-table-query-commands`、`native-system-table-search-negative-row-error`、`native-system-table-byte-row-properties`、`native-system-table-inherited-properties`、`TODO: table-inherited-properties` |
| `表列数` | 初期値はNumberの`1`。各行の`length > cols`が真なら`cols = row.length`と元値をそのまま代入するため、文字列行はUTF-16長、公式生成の関数行は常に`0`、Buffer・Uint8Array行はバイト長、辞書のown `length`は文字列・小数・BigInt・Infinityを含め元の型のまま戻り得る。数値・ArrayBuffer行は無視され、null/undefined行は例外 | AOT・インタプリタとも値の型を保ったままJS relational comparisonと代入を行い、Buffer・Uint8Arrayのbyte lengthとArrayBufferの欠損`length`を公式どおり処理する。標準prototype propertyの代表的な読み出しは実装するが、custom prototypeや未モデルの継承`length`、sparse配列は各TODOとして分離する | `plugin-system-table`、`native-system-table-query-commands`、`native-system-table-byte-row-properties`、`native-system-table-inherited-properties`、`TODO: aot-byte-buffer-value`、`TODO: table-inherited-properties`、`TODO: sparse-array-presence` |
| `表行数` | 配列の`length`だけを返し、行の内容は評価しない | 同じ | `plugin-system-table`、`native-system-table-query-commands` |
| `表列取得` | 各行の`row[col]`をそのまま新配列へ入れる。欠損プロパティは`undefined`となり、null/undefined行では例外。取得したオブジェクト値は元のidentityを保つ。公式生成の匿名なでしこ関数は`name`が空文字列、可変引数ラッパーなので`length`が0。Buffer/Uint8Arrayは`byteLength`・`byteOffset`・`BYTES_PER_ELEMENT`とTypedArray/Bufferのprototype propertyを持ち、ArrayBufferは`byteLength`・`maxByteLength`・`resizable`・`detached`とArrayBufferのprototype propertyを持つ | AOT・インタプリタで配列・辞書・文字列・プリミティブ行と配列の非index own property、関数`name`/`length`を実装し、own propertyを優先して標準prototypeのconstructor・代表的method・`__proto__`を読み出す。ユーザー関数のprototype objectも扱う。Buffer/Uint8Array/ArrayBufferのscalar propertyと代表的prototype methodの読み出しも実装するが、合成methodのreceiver付き呼出しやdescriptor・identity・custom prototype、backing buffer identity、byte bufferの全prototype propertyとsparse配列はTODOで追跡する | `plugin-system-table`、`native-system-table-query-commands`、`native-system-table-column-null-row-error`、`native-system-table-byte-row-properties`、`native-system-byte-buffer-property`、`native-system-table-function-properties`、`native-system-table-inherited-properties`、`native-system-array-own-properties`、`TODO: aot-byte-buffer-value`、`TODO: table-inherited-properties`、`TODO: sparse-array-presence` |
| `表行列交換` | `a[c][r]`を読むため、文字列・辞書行の添字も参照する。欠損（`undefined`）だけを空文字列で補い、null/undefined行のプロパティ参照は例外。空表でも列数初期値1により`[[]]`を返す | AOT・インタプリタで同じ行プロパティ規則と空文字列補完を使い、Buffer/Uint8Arrayの数値添字とArrayBufferの欠損添字も読む。nullish行はエラーにする。無限・100万超の列数は公式の非停止を再現せず安全エラーにする。標準prototype propertyの読み出しは実装するが、custom prototypeや完全な継承semanticsは`TODO: table-inherited-properties`として分離する | `plugin-system-table`、`native-system-table-transform-commands`、`native-system-table-byte-row-properties`、`native-system-table-inherited-properties`、`TODO: table-inherited-properties` |
| `表右回転` | `a[rows-c-1][r]`を読むため、文字列・辞書行の添字も参照し、欠損は`undefined`のまま。null/undefined行のプロパティ参照は例外 | AOT・インタプリタで同じ行プロパティ規則を使い、Buffer/Uint8Arrayの数値添字とArrayBufferの欠損添字も読み、転置と同じ列数安全上限を適用する。標準prototype propertyの読み出しは実装するが、custom prototypeや完全な継承semanticsは`TODO: table-inherited-properties`として分離する | `plugin-system-table`、`native-system-table-transform-commands`、`native-system-table-byte-row-properties`、`native-system-table-inherited-properties`、`TODO: table-inherited-properties` |
| `表重複削除` | 列値を暗黙に文字列化して通常Objectのキーへ入れるため、`__proto__`や`constructor`等のObject.prototype継承キーは最初から使用済みとして全行が落ちる。最初の行を残し、行identityをそのまま返す | Object.prototypeの標準ownキー衝突を明示的に再現し、それ以外は挿入順を保持した辞書で重複を削除する | `plugin-system-table`、`native-system-table-transform-commands` |
| `表列挿入` | `i>0`のときだけ`row.slice(0,i)`を前置し、常に`row.slice(i)`を後置するため、負数は前置なしで末尾基準、小数・NaN・文字列は`slice`のToInteger規則に従う。`s[idx]`の欠損は`undefined`。Buffer行の`slice`は元Bufferとメモリを共有し、Uint8Array・ArrayBuffer行の`slice`はコピーを生成する。空表では列番号とSを評価しない | AOT・インタプリタで同じprefix/suffix規則、非配列`S`の添字、文字列行のUTF-16 slice、byte buffer行の種類と内容を処理する。Bufferは共有storageを使うview、Uint8Array・ArrayBufferは独立copyとして生成し、疎配列holeは`TODO: sparse-array-presence`として分離する | `plugin-system-table`、`native-system-table-transform-commands`、`native-system-table-byte-row-slice`、`native-system-table-byte-row-slice-alias`、`TODO: aot-byte-buffer-value`、`TODO: sparse-array-presence` |
| `表列削除` | 各行を`slice(0)`してから`splice(i,1)`するため、負数・小数・NaN・範囲外はJavaScriptのsplice規則で処理する。空表では列番号を数値化しない | AOT・インタプリタで同じ破壊元を変更しないコピーとsplice位置を使う | `plugin-system-table`、`native-system-table-transform-commands` |
| `表列合計` | 初期値0から各`row[i]`を`+=`するため、文字列は連結、BigIntとNumberの混在は例外、undefinedは`NaN`へ伝播する | JS互換加算（`jsAdd`/`operators.binary`）を使い、Buffer/Uint8Arrayの数値添字とArrayBufferの欠損添字を含む行・列のプロパティ規則とGCルートを保持する | `plugin-system-table`、`native-system-table-transform-commands`、`native-system-table-byte-row-properties` |
| `表曖昧検索` / `表正規表現ピックアップ` | `/式/フラグ`形式ではなく、`RegExp`コンストラクタへ渡す生のパターン文字列を受け取る。`undefined`は空patternになる。`表曖昧検索`は`for (let i=row; i<a.length; i++)`のraw開始値と返却型を保ち、`表正規表現ピックアップ`は各行へ`row.slice(0)`を適用する浅いコピーを返す。Buffer行の`slice(0)`は元メモリを共有し、Uint8Array・ArrayBuffer行の`slice(0)`はコピーを生成する。両命令とも空表でも先に`RegExp`を構築するため、無効patternはループ前に例外になり、null/undefined行は列参照で例外になる | AOT・インタプリタで生patternの一度だけのコンパイル、raw開始値、UTF-16文字列変換、配列・文字列行の浅いコピー、Buffer rowのview共有、GCルートを実装する。通常モードでは共有Zig正規表現実装をAOTへ静的リンクする。標準prototype propertyの読み出しは実装するが、RegExpオブジェクトのglobal/sticky `lastIndex`共有、byte rowを浅くコピーする境界、疎配列hole、custom prototypeや完全な継承semanticsは各TODOとして完成扱いにしない | `plugin-system-table`、`native-system-table-regexp-commands`、`native-system-table-regexp-invalid-pattern-error`、`native-system-table-regexp-null-row-error`、`native-system-table-regexp-buffer-slice-alias`、`native-system-table-inherited-properties`、`TODO: table-regexp-object-last-index`、`TODO: sparse-array-presence`、`TODO: table-inherited-properties`、`TODO: aot-byte-buffer-value` |
| `正規表現マッチ` / `正規表現抽出` / `正規表現置換` / `正規表現区切` | `/式/フラグ`形式を使い、パターン省略時の暗黙`g`、明示フラグ、キャプチャ、名前付きキャプチャ、置換参照、正規表現分割を行う。`u`では補助平面の`/./u`を1文字として扱い、`y`では指定開始位置以外を探索せず、`d`ではフラグを受理する。`抽出文字列`はマッチ・抽出が正常に完了したときだけ更新され、無効patternが例外になった場合は直前の値を保持する | AOTはインタプリタと同じ共有UTF-16バックトラッキングエンジンを静的リンクし、AOT専用ABIで戻り値と`抽出文字列`をGC root付きで更新する。共有エンジンは`u`のraw surrogate pair・`\\u{...}`・dot/class・zero-width advancement、`y`のsticky開始位置を実装する。`d`は受理するがNadesikoの戻り値APIにindicesを公開しない。Unicode property escape、`v`、JSの全てのエラー文言・バックトラッキング境界は完成扱いにしない | `plugin-system-regexp`、`native-system-regexp-commands`、`native-system-regexp-boundaries`、`native-system-regexp-unicode-sticky`、`native-system-regexp-error-preserves-captures`、`TODO: regexp-unicode-flags`、`TODO: regexp-js-error-text`、`TODO: regexp-backtracking-edge` |
| `CSV取得` / `TSV取得` / `表CSV変換` / `CSV変換` / `表TSV変換` / `TSV変換` / `CSVオプション設定` | 公式`core/src/nako_csv.mjs`はCRLF/CRをLFへ正規化して末尾空白を除去し、引用・二重引用符・数値自動変換を行う。変換時は各セルを引用処理後の文字列へ置き換え、オプションはプロセス内状態として保持する | Interpreterは`plugins/csv.zig`、AOTは`runtime/aot.zig`の`AotCsvState`で解析・変換・オプション設定を実装する。通常モードの公式CSV plugin取り込みはnative builtinとして扱い、公式CLI・生成JavaScript・Interpreter・LLVM O0〜O3でfixture範囲を比較する。未fixtureの全てのJavaScript値・空白分類まではこの証拠から保証しない | `plugin-csv-all`、`native-csv-commands`、TODOなし（追加境界は別fixtureで追跡） |

| `TOML取得` / `TOML変換` | 公式`core/src/plugin_toml.mjs`は`smol-toml`へ委譲し、表・配列テーブル・インライン表・文字列・数値・コメントを解析し、辞書をTOMLへ変換する | Interpreterは`plugins/toml.zig`、AOTは`runtime/aot.zig`の`TomlAotParser`とtable writerで実装する。公式CLI・生成JavaScript・Interpreter・LLVM O0〜O3で標準fixture範囲を比較する。未fixtureの全てのsmol-toml拡張値・エラー文言まではこの証拠から保証しない | `plugin-toml-all`、`native-toml-commands`、TODOなし（追加境界は別fixtureで追跡） |
| 辞書リテラルの数値キー | `{1:2}`はJavaScriptのオブジェクト構文なら有効に見えるが、公式パーサーは数値をキーとして受理せず、辞書の閉じ括弧不足に見える文法エラーを返す | 識別子または引用文字列だけを受理し、数値キーは同じ行の構文診断にする | `公式同様に辞書リテラルの数値キーを拒否する`、`parser-diagnostic-cases` |
| 角括弧の分割代入 | `[A,B]=[1,2]`でも分割代入に見えるが、公式パーサーは宣言なしの形を受理しない。`変数[A,B]=[1,2]`または`定数[A,B]=[1,2]`だけが分割宣言になる | 宣言キーワードを必須にし、裸の角括弧形式は同じ行の構文診断にする | `公式同様に宣言なしの角括弧分割代入を拒否する`、`parser-diagnostic-cases`、`native-array-destructure` |
| プリミティブ値への添字代入 | 公式CLI直接実行はNumber、Boolean、String、BigIntへの`A[0]=値`を変更も例外もなく無視する。一方、公式生成JavaScriptはES moduleのstrict modeで動くため、最初のNumber代入から`Cannot create property '0' on number '1'`例外になる。`null`と`undefined`への代入は両経路で`Cannot set properties of null/undefined (setting 'キー')`になる | 通常の利用経路である公式CLI直接実行に合わせ、数値・真偽値・文字列・BigIntへの代入を無操作にする。差分ケースは`oracle: official-source`を明示する。`null` / `undefined`ではキーを文字列化した公式文言を保留例外にし、AOTでも最内側の監視へ渡す | `native-primitive-index-assignment-and-empty-iteration`、`native-nullish-index-assignment-exception`、`プリミティブへの添字代入を無視し非反復値を空として扱う` |
| 反復不能値の`反復` | `null`、`undefined`、Boolean、BigInt、関数は例外にせず要素数0として扱い、反復本体を実行しない | 全ての値を反復入力として受理し、配列・辞書・文字列・Number以外は空反復子へ変換する | `native-primitive-index-assignment-and-empty-iteration`、`プリミティブへの添字代入を無視し非反復値を空として扱う` |

根拠は固定オラクルの
[`core/src/plugin_system_array.mts`](https://github.com/kujirahand/nadesiko3/blob/3.7.24/core/src/plugin_system_array.mts) と
[`core/src/plugin_system_dict.mts`](https://github.com/kujirahand/nadesiko3/blob/3.7.24/core/src/plugin_system_dict.mts) です。

## 文字列

| 構文 | 公式v3.7.24の実際の挙動 | lnakoの扱い | 差分テストID |
|---|---|---|---|
| `出現`の検索対象と配列比較 | 文字列の検索対象はUTF-16コード単位で、空needleは真、needleは文字列化する。配列は`Array.includes`相当のSameValueZeroで、NaN同士と±0を一致させるが、数値1と文字列`"1"`、nullとundefined、内容が同じ別配列は一致せず、同じ配列参照だけ一致する。配列以外の対象は`String(S).includes(A)`になる | AOT・インタプリタとも文字列検索と配列のSameValueZeroを分離し、配列要素の型・参照同一性とUTF-16境界を保持する | `出現の文字列検索と配列SameValueZeroを公式互換にする`、`native-system-occurrence-command` |
| `切取`・`範囲切取`の区切り文字 | 第1引数だけ先に`String`化し、区切り文字A/Bは`indexOf`内で文字列化する。しかし一致後の`substring(i + A.length)`は文字列化後の長さでなく元値の`.length`を読む。数値・真偽値・BigIntの`.length`は`undefined`なので位置は`NaN`から0へ補正され、配列は要素数、関数はarity、辞書は`length`プロパティを使う。`null`/`undefined`は不一致なら無害だが、一致して`.length`を読む時だけTypeErrorになる。範囲版はA不在ならBを変換・参照しない | 専用AOT ABIへ元の区切り値を渡し、UTF-16コード単位の最初の一致を検索してから遅延した`.length`参照を行う。結果とシステムグローバル`対象`は両方の割当が成功した後だけ更新し、ローカル変数名`対象`には影響させない。関数の検索文字列へ名前を含めるAOT値文字列化は共通課題として未完了 | `native-cut-commands`、`native-cut-empty-and-range-boundaries`、`native-cut-user-function-name-precedence`、`native-cut-global-target-with-local-shadow`、`切取系命令はUTF-16と遅延length参照を公式どおり処理する`、`TODO: aot-function-string-name` |
| `切取`・`範囲切取`の不足引数 | 公式CLIは命令定義の引数数を文法検査して実行前に拒否する。公式の生成済みJavaScriptを直接動かした場合も不足値の参照で実行時エラーになる | AOT専用ABIは不足引数を拒否する。lnako全体で組み込み命令の引数数を公式と同じ段階・位置で診断する処理は未完了のため、正常系の互換完了とは分けて追跡する | `TODO: builtin-arity-diagnostics` |
| `文字数`と`LEN` | `文字数`は`Array.from`によるUnicode scalar数なので`文字数("A😀B")`は3だが、`LEN`はUTF-16コード単位数なので4になる | 同じ文字列表現から数え方だけを分け、AOTでも両方を個別の標準命令IDへ下げる | `Unicode文字列命令をコードポイント単位で処理する`、`AOT文字長検索と要素数はUnicode scalarとUTF-16を区別する`、`native-system-string-length-search-boundary-commands` |
| `何文字目`の空検索語 | 空検索語は常に1ではない。公式実装が元文字列の各Unicode scalar位置だけを走査するため、`何文字目("abc","")`は1だが、走査位置が一つもない`何文字目("","")`は0になる | UTF-16の生の`indexOf`へ置き換えず、scalar境界だけを先頭から走査する | `Unicode文字列命令をコードポイント単位で処理する`、`native-system-string-length-search-boundary-commands` |
| `何文字目`の非文字列入力 | 型注釈は文字列だが、実体は両引数へ`Array.from`を適用する。配列は要素列のまま各sliceを`join("")`して検索し、Number・Boolean・BigIntは空列になるため`何文字目("abc",12)`は1、`何文字目(123,12)`は0になる。辞書はownの`length`と数値キーによるarray-like規則、関数は公式wrapperの`length=0`を使う。`null`と`undefined`は`object null is not iterable (cannot read property Symbol(Symbol.iterator))`／`undefined is not iterable (cannot read property Symbol(Symbol.iterator))`になる。未捕捉時は公式CLIがエラーをstdoutへ出して終了コード0、公式生成JavaScript＋Nodeがstderrへ出して終了コード1になる | 対応済みValueモデルの文字列・配列・スカラープリミティブ・own array-like辞書・関数について、InterpreterとAOTの系列化・slice-join検索・監視内のnullish例外・未捕捉失敗を公式7経路で再現する。AOTは暗号命令由来のBuffer/Uint8Arrayを数値要素列として扱うが、ArrayBufferや汎用Array.fromの継承array-likeプロパティ、任意のhostオブジェクト、配列要素内の関数文字列化はそれぞれ`TODO: aot-byte-buffer-value`、`TODO: array-from-inherited-properties`、`TODO: aot-function-string-name`として追跡する。公式の`Array.from`は`length >= 2^32`または`Infinity`で`RangeError: Invalid array length`になる。lnakoは安全性のため100万要素超を早期拒否する意図的制限を持つ。InterpreterとAOTは辞書要素を検索窓ごとに遅延参照し、100万要素以内でも辞書`length`分をeager構築しない。100万要素超の安全上限は意図的制限として維持する | `native-system-string-array-from-coercion`、`native-system-string-array-from-dictionary-lazy-length`、`native-system-string-array-from-null-errors`、`native-system-string-array-from-null-uncaught`、`TODO: aot-byte-buffer-value`、`TODO: array-from-inherited-properties`、`TODO: aot-function-string-name` |
| `文字始`・`文字終`の第1引数 | 第2引数はJavaScriptの`startsWith` / `endsWith`が文字列化するが、第1引数は事前変換されない。したがって`文字始("abc",97)`は偽を返す一方、`文字始(123,12)`は`s.startsWith is not a function`、`文字終(NULL,"x")`はnullのプロパティ参照エラーになる | インタープリタとAOTの両方で第1引数だけ文字列型を要求し、監視時の公式エラー文も区別する | `文字始と文字終は非文字列レシーバを公式文言で拒否する`、`native-system-string-length-search-boundary-commands` |
| `CHR`・`ASC`の補助平面と不正値 | `CHR`は`String.fromCodePoint`相当なのでU+10000以上をサロゲートペアにし、0xD800〜0xDFFFは孤立サロゲートのまま保持する。負数・小数・0x10FFFF超は`Invalid code point 値`となる。`ASC`は先頭のUnicode code pointを返すため、補助平面文字は1値、空文字列は0になる。配列入力では各要素へ同じ変換を行う | UTF-16ヒープ値を直接生成・読取し、孤立サロゲートを途中で置換しない。不正値は固定error名でなく実際の数値を含む動的文言として、インタープリタとAOTの最内側の監視へ渡す | `CHRの不正コードポイントを値付き公式文言で監視する`、`AOT文字コード命令は補助平面と配列と動的例外文言を扱う`、`native-system-character-code-commands` |
| `文字挿入`・`文字検索`の位置 | 位置は`parseInt`でなくJavaScriptの数値演算と配列index変換を通る。`文字挿入("ABC",2.9,"X")`は`AXBC`、数値化不能な`"2rest"`は先頭挿入になる。一方、`文字検索("A😀B😀",2.9,"😀")`は走査中の小数を戻り値にも保持して`2.9`となり、`"2rest"`は走査せず0になる。位置はUTF-16単位でなくUnicode scalar単位 | インタープリタとAOTで、挿入時だけindexを0方向へ整数化し、検索ループの位置値は小数のまま進める。数値化不能位置を接頭辞整数として受理しない | `文字列挿入検索は公式の小数位置とNaN位置を保持する`、`AOT文字列挿入検索はUnicode scalar位置と小数開始値を保持する`、`native-system-string-position-commands` |
| `連結`のnullish要素 | 内部実装が可変引数配列の`join("")`を使うため、`null`と`undefined`は文字列`"null"`・`"undefined"`にならず空文字列になる。`連結("a",1,NULL,undefined)`は`"a1"` | 通常の値文字列化を単純に連ねず、`連結`・`文字列連結`だけnullish要素を空として扱う | `文字列連結反復出現は公式のnullと小数と空区切りを扱う`、`AOT文字列連結分解反復出現命令は公式の型変換を保つ`、`native-system-string-composition-commands` |
| `リフレイン`の小数回数と`出現回数`の空検索語 | `リフレイン`は整数化せず`for (i < cnt)`で反復するため、正の小数2.1は3回になる。数値化不能文字列では0回。`出現回数`は`split(A).length-1`なので空検索語ではUTF-16長から1を引き、`出現回数("😀","")`は1、空文字列同士では`-1`になる | 小数回数は正の有限値を上方向へ丸め、無限反復は拒否する。空検索語の出現数はUnicode scalar数で補正せず、UTF-16コード単位長と空入力の負値を保持する | `文字列連結反復出現は公式のnullと小数と空区切りを扱う`、`AOT文字列連結分解反復出現命令は公式の型変換を保つ`、`native-system-string-composition-commands` |
| `文字抜出`・`MID`の数値型と位置0 | 開始・文字数が文字列のときだけ先に`parseInt`するが、Numberの小数はそのまま`Array.slice`へ渡す。よって数値`2.9, 2.9`は3文字、文字列`"2.9", "2.9"`は2文字を返す。開始0は先頭へ補正されず`slice(-1, …)`となり、通常は空になる。`RIGHT`も`長さ-CNT`を整数化するため小数2.9で末尾3文字になる | 引数タグによる変換差とJavaScriptの負index・0方向丸めをインタープリタとAOTで明示的に再現し、単純な整数位置へ正規化しない | `部分文字列命令は数値小数と文字列小数と位置0を区別する`、`AOT部分文字列命令は数値小数と文字列小数を区別する`、`native-system-substring-commands` |
| `区切`の空区切りと`文字削除`の位置 | `区切`はJavaScript文字列の`split`なので、空区切りではUnicode scalarでなくUTF-16コード単位に分かれ、絵文字は孤立サロゲート2要素になる。`文字削除`は`Array.from`後の`splice(A-1,B)`であり、A=0は末尾1文字、A=-1は末尾2文字を削除する。`"2rest"`は位置NaN→0、削除数NaN→0となり何も削除しない | 分割の空区切りだけUTF-16コード単位を使い、通常区切りと削除対象はUnicode scalar境界を使う。削除位置・件数は`parseInt`せずspliceの相対負indexとNaN規則を再現する | `文字削除はspliceの負位置と数値化不能削除数を扱う`、`AOT文字列分割削除はUTF-16空区切りとsplice位置を扱う`、`native-system-string-split-remove-commands` |
| トリム系の空白集合 | `トリム`・左右トリム系はJavaScript正規表現の`\\s`に一致するUTF-16コード単位を除去する。ASCII空白だけでなくNBSP、全角空白、行・段落区切り、BOMも対象だが、ゼロ幅スペースU+200Bは残る | ECMAScriptの固定空白集合を共有し、ロケールやUnicode一般カテゴリへ依存しない。左右・両側と全別名をAOT命令として個別に解決する | `AOTトリム命令はECMAScript空白だけを左右別に除去する`、`native-system-trim-commands` |
| `大文字変換`・`小文字変換` | JavaScriptの`toUpperCase` / `toLowerCase`相当のUnicode完全写像を使う。`ß`などの1対多写像、補助平面、孤立サロゲートを保持し、小文字化ではUnicodeの文脈依存final sigmaも適用する | Node 24.15.0から生成した写像表と、ケース無視文字を飛ばす前後の文脈判定をAOTでも共有する。UTF-16コード単位を壊さず、公式の文字列化・表示境界へ渡す | `native-system-case-conversion-commands` |
| `平仮名変換`・`カタカナ変換` | ひらがな`U+3041..U+3096`とカタカナ`U+30A1..U+30F6`だけを固定オフセットで相互変換し、範囲外のカナ拡張や補助平面は変更しない | インタープリタと同じ両端を含む範囲変換をAOTへ実装し、UTF-16の他のコード単位をそのまま保持する | `native-system-case-conversion-commands` |
| `英数全角変換`・`英数半角変換` | 英数だけの全角化・半角化はASCII英字数字と対応するFF21..FF3A、FF41..FF5A、FF10..FF19だけを変換し、記号と空白は対象外。記号版はASCII空白`U+0020`と`U+0021..U+007E`を全角化し、半角化では`U+3000`と`U+FF00..U+FF5F`（両端を含む）を半角化するため、範囲端`U+FF00`は空白、`U+FF5F`は`U+007F`になる | 公式と同じ非対称な範囲・UTF-16単位の変換を行い、補助平面と孤立サロゲートは保持する | `native-system-width-conversion-commands` |
| `カタカナ全角変換`の濁点表検索 | 半角濁点表を2文字候補だけでなく末尾1文字でも検索するため、単独の`ﾞ`は`ガ`、単独の`ﾟ`は`パ`になる。2文字の`ﾞﾟ`は先頭が空文字になり末尾だけが`パ`となり、`ｶﾞﾊﾟﾞﾟ`は`ガパパ`になる。表の奇数位置にも一致するので`ﾞｷ`は`ガ`、`ﾞﾀ`は`ゾ`、`ﾞﾊ`は`ド`となり、末尾の単独`ﾂ`も`ヅ`になる。これは範囲外保持の一般則に対する公式実装固有の例外 | AOTも公式の2文字優先検索、位置の2除算切り捨て、末尾1文字検索、表の範囲外インデックスを空文字として扱う挙動を再現し、孤立サロゲートや他の範囲外コード単位だけを保持する | `native-system-width-conversion-commands` |
| `全角変換`・`半角変換`の合成順序 | 両命令はカナ変換を先に行い、続けて英数記号変換を行う。そのため`全角変換("A ｶﾞ!")`は`Ａ　ガ！`、`半角変換("Ａ　ガ！")`は`A ｶﾞ!`になる | 中間文字列をGCルートへ保持して同じ順序で純LLVM AOTへ下げる | `native-system-width-conversion-commands` |
| 幅変換命令の非文字列引数 | ASCII4命令（`英数全角/半角変換`・`英数記号全角/半角変換`）は公式どおり`String(s)`へ変換する。カナ全角化と`全角変換`は生の`length`を参照し、Number・真偽値・BigInt・空配列・lengthなし辞書・生成関数（wrapperのlength=0）は空文字列、非空配列・own `length > 0`の辞書は`s.substring is not a function`、nullishは`length`参照エラーになる。カナ半角化と`半角変換`は生の`split`を呼ぶため、文字列以外はnullishの専用参照エラーまたは`s.split is not a function`になる。辞書のlength判定はJS抽象比較で行う | インタープリタとAOTの両方で上記のraw receiver分岐、専用エラー文言、合成順序を実装済み。動的にown `substring`/`split`メソッドを呼び出す辞書・オブジェクト表現は未対応であり、継承プロパティも探索しない。Buffer・Uint8Array・ArrayBufferはインタープリタで分岐を実装し、AOTは暗号命令由来のバイト値を保持するが、これらのraw receiver method分岐は`TODO: aot-byte-buffer-value`として未実装境界に残す | `native-system-width-ascii-non-string-coercion`、`native-system-width-kana-full-non-string-coercion`、`native-system-width-kana-half-non-string-coercion`、`native-system-width-function-receiver`、`native-system-width-half-uncaught-error`、`TODO: system-width-custom-string-methods`、`TODO: aot-byte-buffer-value` |
| `置換`と`単置換`の置換文字列・`undefined` | 全置換の`置換`は`split(A).join(B)`なのでBを常にリテラルとして使い、空検索語では文字間だけへ挿入する。Aが`undefined`なら分割せず、Bが`undefined`なら`join`既定区切りのカンマを使う。一方、`単置換`はJavaScript `replace`なので`undefined`を文字列検索し、`$$`を`$`、`$&`を一致文字列、``$` ``を一致前、`$'`を一致後へ展開する | 全置換と単置換で型強制と置換文字列展開器を共有せず、空検索語の端処理も分ける。正規表現のcapture展開とは別の文字列検索専用経路としてインタープリタとAOTへ実装する | `置換はsplitとjoinのundefined型強制を再現する`、`単置換の置換パターンと全置換の空検索語を公式通り処理する`、`AOT置換命令は全置換の空検索と単置換の置換パターンを分ける`、`native-system-replace-commands` |
| `通貨形式`の区切り位置 | 数字列の先頭を探す単純な数値フォーマットではなく、ASCII数字の直後に3桁単位の数字列が続く箇所へ正規表現でカンマを挿入する。ドットとその後の数字列に対する可変長後読みがあるため、小数部へは挿入しないが、`abc1234567xyz1234`のように複数の数字列がある場合は各数字列を個別に処理する。全角数字は対象外 | 公式正規表現の先読み・後読みをUTF-16コード単位の走査へ展開し、ASCII数字だけを同じ位置へ挿入する | `指定形式と文字種判定の公式境界を処理する`、`native-system-format-and-character-predicate-commands` |
| `ゼロ埋`・`空白埋`の幅変換 | 幅の比較前に`for (i < A)`を実行し、その後`parseInt(A)`を使うため、`"2rest"`や小数は走査回数と最終幅で異なる。数値化不能値は最初の埋め文字1個が残る一方、真値は前置きループを1回通ってから`parseInt(真)`が`NaN`になり、2個残る。`Infinity`は公式実装の前置きループが終了せず、実行を停止させ得る | 有限値、BigInt、接頭辞整数、真偽値、`NaN`を公式どおりに処理する。AOTでの無限ループは安全上再現せず、Infinity、`f64`へ収まらない巨大BigInt、割当不能な有限幅は`OutOfMemory`相当へ制限する | `指定形式と文字種判定の公式境界を処理する`、`TODO: system-string-pad-infinity-width`、`native-system-format-and-character-predicate-commands` |
| `数列判定`の正規表現境界 | 元引数が空文字列のときだけ先に`false`を返すが、正規表現自体は空にも一致する。そのため`[]`、`[""]`、`[NULL]`のように文字列化後だけ空になる値は`true`になる。`+`、`-`、全角符号だけでも一致し、整数、先頭省略小数、全角数字・小数点・指数を受理するが、指数は小数部がある場合だけなので`1e3`や`1.`は不一致 | 元値の型を保ったまま空文字列だけを先に除外し、その後は公式正規表現の構造をAOTとインタープリタで共有する | `指定形式と文字種判定の公式境界を処理する`、`native-system-format-and-character-predicate-commands` |
| 補助平面文字の添字と`反復` | 文字列をUnicode文字数ではなくUTF-16コード単位で扱う。`「A😀B」`の添字と反復キーは0〜3になり、1と2の値は高位・低位サロゲート単独になる。CLIが各単独値をUTF-8へ出力するとそれぞれ`�`へ置換される | AOTを含む全経路でUTF-16ヒープ文字列を使い、添字・反復・孤立サロゲートの出力置換を同じ境界にする | `UTF-16文字列の添字と反復をコード単位で処理する`、`native-utf16-string-index-and-foreach` |
| 文字列中のNUL | `U+0000`は文字列の終端ではなく通常のUTF-16コード単位であり、後続文字も保持・出力する | CのNUL終端文字列として埋め込まず、長さ付き`i16`定数からヒープ値を生成する | `UTF-16文字列定数と添字と反復をAOTランタイムへ変換する`、`native-utf16-string-index-and-foreach` |
| 展開あり文字列の中括弧 | `「...」`、`"..."`などの`{...}`は単な文字置換ではなく、内側を通常のなでしこ式として再度字句解析する。全角`｛...｝`も同じ。開き括弧の後ろで最初の`}`/｝を閉じとし、余分な閉じ括弧は文字のまま残るが、閉じのない開き括弧はエラーになる。`『...』`と`'...'`は展開しない | 構文変換で埋め込み式を再度トークン化し、括弧付きの`&`連結へ展開する。その後は通常の意味解析・SSA・AOT値文字列化を共用する | `展開あり文字列の埋め込み式を文字列連結へ変換する`、`native-string-template` |

根拠は固定オラクルの[`NakoLexer.splitStringEx`](https://github.com/kujirahand/nadesiko3/blob/3.7.24/core/src/nako_lexer.mts)と、
そこで作られた`code`トークンを再度字句解析する[`NakoTokenizer.lex`](https://github.com/kujirahand/nadesiko3/blob/3.7.24/core/src/nako_tokenizer.mts)です。

## 比較・単項演算

| 構文 | 公式v3.7.24の実際の挙動 | lnakoの扱い | 差分テストID |
|---|---|---|---|
| 配列・辞書の`==`と`===` | 両辺が配列・辞書なら内容比較や文字列変換をせず、参照が同一かを判定する。同じ内容の別配列`A=[1]; C=[1]`では`A==C`も`A===C`も偽だが、`B=A`なら両方とも真。一方、片辺だけがオブジェクトの`[1]==1`では配列を文字列`「1」`へ変換するため真になる | インタプリタとAOTで、両辺がオブジェクトなら参照同一性を先に判定し、片辺だけならJavaScriptのToPrimitive相当を適用する | `配列の伸長と挿入順辞書の更新を扱う`、`AOT動的比較は文字列変換と参照同一性を区別する`、`native-dynamic-comparison` |
| `一致` / `不一致` | 公式`core/src/plugin_system_math.mts`は、左辺がオブジェクト（配列・辞書・nullなど）の場合に両辺を`JSON.stringify`した結果で比較し、その他はstrict equalityを使う。辞書キー順、配列内のundefined/非数、辞書内のundefined省略、BigInt/循環値のJSON変換境界は値型ごとに異なる | InterpreterとAOTの`一致`/`不一致`は左辺がJSON.stringify対象なら共有JSON規則で両辺を直列化し、文字列をstrict equality比較する。左辺がプリミティブなら公式どおり直列化せずstrict equalityへ戻る。JSON変換のBigInt・循環参照エラーも伝播し、null・配列・辞書・byte buffer・Promise（AOTではiteratorも）のobject境界を対象にする。QuickJS互換モードは対象外 | `plugin-system-math`、`native-system-deep-comparison` |
| `+`と他の数値演算の変換差 | `+`だけは非数値オペランドを公式の`__parseFloatOrBigint`で`parseFloat`してから加算する。そのため`"5x"+2`は`7`だが`"5x"-2`は`NaN`、`[]+1`は`NaN`だが`[]-1`は`-1`になる。文字列結合は`+`ではなく`&`が担う | 動的AOT加算はECMAScript `parseFloat`相当、その他の演算はToNumber相当を別経路で実行し、インタプリタと一致させる | `なでしこ式の加算は文字列を連結せず数値へ変換する`、`AOT動的数値演算は文字列・配列・辞書を公式規則で変換する`、`native-dynamic-arithmetic` |
| 整数・実数変換の末尾文字 | `整数変換` / `TOINT` / `INT`は`parseInt`、`実数変換` / `TOFLOAT` / `FLOAT`は`parseFloat`相当で、数値接頭辞の後ろにある変換不能文字を無視する。空文字は0でなく`NaN`になる | UTF-16空白除去と接頭辞解析をインタープリタ・AOTで共有し、BigInt、配列の文字列化、16進接頭辞、Infinityも同じ規則で処理する | `parseIntとparseFloatのJavaScript接頭辞規則を再現する`、`native-system-number-conversion-commands` |
| `NAN判定`と`非数判定` | `NAN判定`はグローバル`isNaN`相当で、`undefined`や`"12x"`をNumber変換して真、空文字や空配列を0へ変換して偽にし、BigIntでは例外になる。`非数判定`は`Number.isNaN`相当で、Number型のNaNだけが真になる | AOTでもToNumberを行う経路と型タグだけを見る経路を分離し、BigIntの変換失敗は保留例外として最内側の監視へ渡す | `AOTのNAN判定と非数判定はNumber変換の有無を区別する`、`native-system-nan-predicates` |
| `HEX`・`二進`・`進数変換`の変換順 | 値を`parseInt`してからNumberの`toString(radix)`を呼ぶため、入力値の小数部と末尾の変換不能文字を捨てる。基数の小数部も切り捨てるが、2未満・36超・NaN・InfinityはRangeErrorになる。明示的な`undefined`だけは基数省略扱いの10進になる。`二進表示`だけは文字列を返さず、その結果を改行付きで表示する | 接頭辞整数解析と基数文字列化をインタープリタ・AOTで共有し、AOTの副作用と例外文言も公式Node 24へ合わせる | `整数を2進数から36進数の小文字表現へ変換する`、`AOT進数変換は小数基数を切り捨てて不正基数を例外にする`、`native-system-radix-commands` |
| `RGB`の範囲外成分 | 各成分を0〜255へクランプせず、`parseInt(String(V)).toString(16)`の先頭へ`00`を足して末尾2文字を取る。したがって`RGB(-1,非数,無限大)`は`#-1aNaN`、256は`00`になり、有効なCSS色にならない場合がある | インタープリタとAOTで同じ末尾抽出を共有し、通常値だけでなく負数、NaN、Infinity、256以上も公式出力と比較する | `RGBは各parseInt結果の16進表現から末尾2文字を取る`、`AOTのRGBは各16進表現の末尾2文字を連結する`、`native-system-rgb-command` |
| NumberとBigIntのビット命令 | Numberの`OR`・`AND`・`XOR`・`NOT`はオペランドを符号付き32bitへ変換し、シフト量は下位5bitだけを使う。文字列や単一要素配列もNumberへ変換する。一方、両辺がBigIntなら任意精度で処理し、Numberとの混在とBigIntの`SHIFT_UR`は例外になる | 標準命令と構文演算子で同じToPrimitive・ToNumber・BigInt経路を共有し、例外を最内側の監視へ渡す | `AOTビット命令はNumberの32bit化とBigInt演算を区別する`、`native-system-bitwise-commands` |
| `べき乗`命令と`^` / `**` | 構文演算子の`^` / `**`はBigInt同士の冪乗を扱えるが、標準命令`べき乗`は公式の`Math.pow`を呼ぶため、BigIntをNumberへ変換できず例外になる | インタープリタとAOTの`べき乗`だけをNumber限定経路にし、構文演算子の任意精度経路と分離する | `AOT算術比較命令は奇数の符号とNumber限定べき乗を保持する`、`native-system-arithmetic-comparison-commands` |
| 負の値の`奇数`判定 | `parseInt(V) % 2 === 1`をそのまま使うため、`奇数(5)`は真だが、剰余が`-1`になる`奇数(-3)`は偽になる | 絶対値や下位bitで補正せず、JavaScriptと同じ符号付き剰余を1と比較する | `AOT算術比較命令は奇数の符号とNumber限定べき乗を保持する`、`native-system-arithmetic-comparison-commands` |
| `論理OR`・`論理AND`の戻り値 | 真偽値へ正規化せず、`論理OR(0,"右")`は`"右"`、`論理OR("左","右")`は`"左"`、`論理AND("左","右")`は`"右"`を返す。空配列・空辞書も真、空文字・`0n`・NaNは偽になる | AOTの条件分岐と同じtruthinessを使い、選ばれたオペランドのタグ付き値をそのまま返す | `AOT集約論理範囲命令は動的値と辞書を返す`、`native-system-aggregate-logical-range-commands` |
| 空コレクションの真偽と同一性 | `空配列()`・空辞書系命令は呼ぶたびに別オブジェクトを返すが、中身が空でも条件上は真になる。`真偽判定([])`と`真偽判定({})`はいずれも`"真"`になる | AOTで呼び出しごとにGC管理オブジェクトを生成し、ポインタの有無ではなく共通truthiness規則で日本語ラベルを返す | `AOT空コレクション命令は独立値を返し真偽判定は日本語ラベルにする`、`native-system-empty-collection-truth-command` |
| `掛`の左辺型と反復回数 | 左辺が文字列または配列なら乗算でなく反復になり、回数は`parseInt`されるため`"3rest"`は3回、小数は切り捨て、0・負数・NaNは空結果になる。その他の型は通常の数値・BigInt乗算になる | UTF-16文字列と配列はGCルートを保持して新しい値へ複製し、その他は動的算術経路へ渡す | `AOT掛命令は文字列配列反復と数値乗算を切り替える`、`native-system-repeat-multiply-command` |
| `足`のBigInt変換 | 通常は両辺を`parseFloat`して数値加算するが、どちらか一方でもBigIntなら両辺へ`BigInt(...)`を適用する。したがって`足("2",3n)`は`5n`相当になる一方、`足(NULL,3n)`は`Cannot convert null to a BigInt`になる | AOTでもBigIntの存在を先に調べ、文字列・真偽値・配列をToPrimitive後に任意精度整数へ変換する。nullとundefinedは公式文言の保留例外にする | `AOT加算系命令はparseFloatとBigIntとJavaScript加算を分離する`、`native-system-addition-commands` |
| `合計`の先頭配列 | 第1引数が配列なら後続引数をすべて無視して`配列合計`へ委譲し、配列要素を`parseFloat`したNaNも0相当として飛ばす。`合計([1,"x",2],100)`は3になる。通常の可変引数経路ではNaNを飛ばさない | インタープリタとAOTの両方で先頭値の型を先に分岐し、配列経路だけ後続無視・NaN除外を行う | `合計は先頭配列だけを集計しNaNを飛ばす`、`native-system-addition-commands` |
| `連続加算`の順序と0引数 | 第1引数を先頭から外して第2引数以降をJavaScriptの`+`で連結・加算し、最後に第1引数を足すため、`連続加算("a","b","c")`は`"bca"`になる。0引数では内部`sys`が第1引数として露出し、共有オブジェクトを返す | 数値加算専用のなでしこ`+`経路を使わずToPrimitive付きJS加算を使う。0引数は各呼び出しで同じGCルート付きシステム文脈を返す | `引数なし連続加算は共有システム文脈を返す`、`AOT加算系命令はparseFloatとBigIntとJavaScript加算を分離する`、`native-system-addition-commands` |
| `MAX`・`MIN`のNaNと後続変換 | NaNを含めば結果はNaNだが、その後ろの引数もNumber変換するため、`MAX(非数,1n)`はNaNを返さずBigInt変換例外になる | 全引数を順にNumber変換し、変換成功後にNaN結果を確定する。Zigの`@max` / `@min`固有のNaN選択には依存しない | `AOT集約論理範囲命令は動的値と辞書を返す`、`native-system-aggregate-logical-range-commands` |
| `÷÷`の負数丸め | 「0方向へ切り捨て」ではなく、公式生成JavaScriptの`Math.floor(left / right)`どおり負の無限大方向へ丸める。`(-5)÷÷2`は`-3` | インタプリタ、動的AOT、数値型が証明済みのLLVM最適化経路の全てでfloorに統一する | `native-dynamic-arithmetic` |
| `かつ` / `&&`と`または` / `||` | 真偽値へ正規化せず、`and`は偽の左辺または右辺、`or`は真の左辺または右辺の値そのものを返す。結果が左辺で決まる場合、表示や関数呼び出しを含む右辺は実行しない | HIRの両辺を先に評価せず、SSA IRで右辺専用ブロックとPHIを生成して値と副作用を両方一致させる | `論理演算の右辺を短絡分岐とPHIへ変換する`、`native-logical-short-circuit` |
| 単項`+` | JavaScriptでは数値変換演算子だが、なでしこ3の`+`は二項演算子だけであり、`+1`、`+A`、`+「1」`はいずれも`+`付近の文法エラーになる | 値変換へ下げず、`+`のソース位置を持つ構文診断として拒否する | `公式同様に単項プラスを拒否する`、`parser-diagnostic-cases` |

固定した公式処理系は[`nako_gen.mts`](https://github.com/kujirahand/nadesiko3/blob/3.7.24/core/src/nako_gen.mts)で
比較をJavaScript演算子へ生成します。上表は公式CLIと生成済みJavaScriptの両方をオラクルにして固定します。

## 標準出力

| 命令 | 公式v3.7.24の実際の挙動 | lnakoの扱い | 差分テストID |
|---|---|---|---|
| `継続表示` | 直ちには出力せず内部プールへ文字列を追加し、次の`表示`がプールと引数をまとめて1行出力する | 同じ | `plugin-system-stdio` |
| `連続無改行表示` | 可変個引数を結合して`継続表示`へ渡すため、次の`表示`まで出力を保留する | 同じ | `plugin-system-stdio` |
| `言` | 命令本体は文字列だけをロガーへ送るが、公式CLIのstdoutロガーを通すと1行出力になり改行が付く。`表示ログ`には追加しない | CLIで同じ1行出力にする | `plugin-system-stdio` |
| `表示` | 保留中の継続表示を先頭に付けて出力し、改行付きの内容を`表示ログ`へ追記する | 同じ | `plugin-system-stdio` |
| 式中の`表示(...)`の戻り値 | 引数を出力するが、その引数値は返さず`undefined`を返す。`1かつ表示("右")`は右辺の表示後、式全体の値が`undefined`になるが、システム変数`それ`は更新しない。命令定義の`return_none: true`により、戻り値あり命令と生成コードが分かれるためである | AOTの表示ヘルパーも出力後に`undefined`を返す一方、`return_none`相当の命令は`それ`へ書き戻さない | `native-logical-short-circuit`、`関数の戻り値だけをシステム変数それへ書き戻す`、`native-function-result-variable` |
| 配列・辞書の`表示` | 配列は要素をカンマで連結し、辞書は内容ではなく`[object Object]`と表示する。自己循環した配列は循環箇所を空文字にするため、自分自身だけを持つ配列の表示結果は空行になる | AOTでも循環検出付きのUTF-16文字列化を通してから出力する | `AOTの値をUTF-16文字列として連結する`、`native-array-and-dictionary` |

根拠は固定オラクルの
[`core/src/plugin_system_stdio.mts`](https://github.com/kujirahand/nadesiko3/blob/3.7.24/core/src/plugin_system_stdio.mts) です。

## 関数呼び出し

| 構文 | 公式v3.7.24の実際の挙動 | lnakoの扱い | 差分テストID |
|---|---|---|---|
| 静的に解決するユーザー関数の引数個数差 | JavaScriptの通常の関数呼び出しと異なり、不足引数を`undefined`で補ったり余分な引数を無視したりしない。定義と呼び出しの個数が違うと、公式コンパイラが実行前に文法エラーとして拒否する | 関数シンボルの形式引数数を意味解析で保持し、直接呼び出しの実引数数が異なる場合は`invalid_argument_count`診断でIR生成前に拒否する | `静的に解決したユーザー関数の引数個数差を拒否する`、`semantic-diagnostic-function-arity-missing`、`semantic-diagnostic-function-arity-extra` |
| 動的なユーザー関数の引数個数差 | 関数値の動的呼び出しでは静的呼び出しと逆に、不足・超過を拒否しない。実引数列の末尾へ内部`sys`オブジェクトを追加してから通常のJavaScript引数規則を適用するため、2引数関数の`F()`は第1引数が`sys`、第2引数が`undefined`、`F(1)`は第2引数が`sys`になる。超過分は無視する。同じ`sys`を呼び出し間で共有する | ネイティブ側では内部プロパティを公開しない共有辞書値を`sys`の不透明な代替値とし、表示の`[object Object]`、参照同一性、不足位置への追加、残りの`undefined`、超過引数無視を再現する | `native-dynamic-function-arity`、`AOT動的関数の不足引数へ共有システム文脈を追加し超過引数を無視する` |
| 裸の名前付き関数 | 式中の名前付き関数`F`は関数オブジェクト参照ではなく暗黙呼び出しになる。0引数関数はそのまま呼び、1引数関数は不足値を現在の`それ`で補うため、`TYPEOF(F)`もFの戻り値の型を返す。2引数以上を引数なしで置くと文法エラーになる | 意味解析で裸の関数記号を通常の変数参照と区別し、0引数または`それ`を渡す1引数の静的呼び出しへ下げる。2引数以上はIR生成前に拒否する | `native-bare-named-function-call`、`semantic-diagnostic-bare-function-arity`、`裸の名前付き関数は1引数以下だけ暗黙呼び出しとして解決する`、`裸の1引数関数をそれ付き暗黙呼び出しへ下げる` |
| ユーザー関数の呼び出し結果と`それ` | ユーザー関数の戻り値をシステム変数`それ`へ毎回書き戻す。空関数など結果指定がない関数は以前の`それ`を引き継がず`undefined`を返すが、例外終了した関数は`それ`を更新せず呼び出し前の値を残す。末尾の`それは式`は`それ`代入と関数結果の両方になる。組み込み`表示`のように、戻り値を持っていても`それ`を更新しない命令とは別規則 | AOTで`それ`を常設のGCルート付きグローバルとし、ユーザー関数呼び出し後だけ戻り値を書き戻す。終端が`それ`代入ならそのSSA値、それ以外の正常な戻り値なし終端なら`undefined`を返し、保留例外があれば旧値を選ぶ | `関数戻り値をシステム変数それへ書き戻す`、`native-function-result-variable`、`native-bigint-runtime-exception-monitor` |
| クロージャのローカル変数捕捉 | 無名関数作成時の値をコピーするのではなく、外側の可変な束縛を共有する。作成後に外側が更新した値が見え、クロージャ自身の更新も次回呼び出しまで保持される。内側のクロージャだけが参照する変数も、中間のクロージャが束縛を中継する | インタプリタとAOTの両方で、GC管理の可変セルを外側フレームと各クロージャが共有する。関数値は捕捉セルをGCで追跡し、LLVMの統一コールバックABIは関数コンテキストから同じセルを復元する | `クロージャが外側の可変束縛を共有する`、`closure-shared-mutable-binding`、`AOTクロージャがGC管理の可変セルを共有する`、`native-closure-shared-mutable-binding`、`native-closure-transitive-capture` |

## Promise

| 命令・境界 | 公式v3.7.24の実際の挙動 | lnakoの扱い | 差分テストID | TODO識別子 |
|---|---|---|---|---|
| `動時`・`成功時`・`処理時`・`失敗時`・`終了時` | Promiseのexecutorは同期的に実行され、解決・拒否後の反応はFIFOのmicrotaskとして実行される。`成功時`と`失敗時`は対応する値を受け取り、`処理時`は成否booleanと値を受け取り、`終了時`は成否にかかわらず元の状態と値を次へ伝播する | Interpreterと純LLVM AOTで`pending` / `fulfilled` / `rejected`、反応列、FIFOマイクロタスク、次のPromiseへの成功・失敗・finally伝播を実装する。AOTはJS runtimeを使わずresolver関数をRuntimeのGC対象関数値として保持する。QuickJS互換モードはcore Promiseのnative証拠経路に含めない | `native-system-promise-success`、`native-system-promise-reject-process-finally`、`native-system-promise-reject` | なし |
| `束` | `Promise.all`相当で入力の順序を保った配列を返し、入力が空なら空配列を即時に解決する。入力Promiseが拒否されると最初の拒否値で束全体を拒否する | Interpreterと純LLVM AOTで入力順の結果配列、空入力、最初の拒否を処理する。AOTは各入力へ専用handlerを接続し、結果配列と残件数をGCルートとして保持する | `native-system-promise-bundle` | なし |
| `AWAIT実行`のPromise待機 | `AWAIT実行`は第2引数を展開して関数を呼び、戻り値がPromiseなら解決まで待機して値を返す。Promiseでない戻り値はそのまま返す | Interpreterと純LLVM AOTで同じ引数展開を行い、AOTはPromiseマイクロタスクとタイマーをドレインしてfulfilled値を返す。拒否時は保留例外を上書きせず、既存のエラー経路へ渡す。通常モードへJavaScript runtimeを混入させず、QuickJSは対象外 | `native-system-promise-timer-await`、`native-system-execute` | なし |

## 例外監視

| 構文 | 公式v3.7.24の実際の挙動 | lnakoの扱い | 差分テストID |
|---|---|---|---|
| `エラー監視`内の`エラー発生` | `エラー発生`より後にある監視本体は実行せず、最も内側の`エラーならば`へ移って`エラーメッセージ`に`new Error(s).message`相当の文字列を設定する。`undefined`は空文字になり、それ以外の値はJavaScriptの文字列化結果になる。呼び出し先で投げた値も呼び出し元の監視へ伝播し、ハンドラ終了後は監視構文の後から実行を続ける | インタプリタとAOTで同じ。AOTランタイムは保留例外をGCルートとして保持し、SSAの呼び出し後分岐で関数フレームを越えて最内側ハンドラへ渡す。配列代入や反復初期化など、二項演算以外の命令が生じる実行時エラーを同じ機構へ渡す処理は未実装 | `native-exception-monitor`、`native-nested-exception-monitor`、`native-cross-function-exception`、`native-system-error-raise`、`保留例外をGCルートとして保持し一度だけ取り出す` |
| BigInt二項演算の実行時エラー | 監視内ならJavaScriptの例外文を`エラーメッセージ`へ格納する。型混在は`Cannot mix BigInt and other types, use explicit conversions`、`÷÷`は`Cannot convert a BigInt value to a number`、符号なし右シフトは`BigInts have no unsigned right shift, use >> instead`となる | JSランタイムを使わない通常モードでも同じ固定文言へ変換する。二項演算直後のSSA例外分岐と保留例外により、呼び出し先で生じた失敗も呼び出し元の監視へ伝播する | `native-bigint-runtime-exception-monitor`、`BigInt実行時エラーを公式JavaScriptの文言へ変換する`、`AOT算術失敗を公式文言の保留例外へ変換する` |
| 監視外の`エラー発生` | 公式`cnako3 source.nako3`はエラー文を標準出力へ出して終了コード0になる一方、公式生成JavaScript＋Nodeは未捕捉例外で終了コード1になる | `lnako run`とAOTは公式生成JavaScript側に合わせて終了コード1にする。差分ハーネスでは`oracle: official-generated`を明示し、公式CLIとの差を隠さない | `native-uncaught-exception`、`監視外のthrowを保留例外としてmainまで伝播する` |

## BigInt

| 構文 | 公式v3.7.24の実際の挙動 | lnakoの扱い | 差分テストID |
|---|---|---|---|
| BigIntリテラルの`表示` | ソースでは末尾に`n`を付けるが、表示時は10進へ正規化して`n`を出力しない。16進・2進・8進や桁区切りも10進表示になる | 任意精度値のまま保持し、同じ10進文字列を出力する | `AOT BigIntを任意精度で生成して真偽判定する`、`native-bigint-literals-and-truthiness` |
| `0n`の条件判定 | 通常のオブジェクト値と異なり`0n`だけが偽、他のBigIntは真になる | AOTでもポインタ先の任意精度値を検査し、同じ真偽値にする | `BigInt定数と真偽判定をAOTランタイムへ変換する`、`native-bigint-literals-and-truthiness` |
| BigIntの除算・剰余 | `/`は0方向へ整数化し、`%`の余りは被除数と同じ符号になる。例えば`-5n/2n`は`-2`、`-5n%2n`は`-1` | 浮動小数点数へ変換せず、任意精度整数の0方向除算を行う | `AOT BigInt算術とNumber混在エラーを処理する`、`native-bigint-arithmetic-and-comparison` |
| BigIntの`÷÷` | 固定した公式v3.7.24でも経路により異なる。`cnako3 source.nako3`はエラー文を標準出力へ出して終了コード0、公式生成JavaScript＋Nodeは`Cannot convert a BigInt value to a number`例外で終了コード1になる | 値を誤生成せず、公式生成JavaScriptと同じ実行時エラーとして拒否する。公式2経路が一致しないため成功差分テストから分離する | `AOT BigInt算術とNumber混在エラーを処理する` |
| 負のBigInt | 文や代入式の先頭にある`-5n`は負のBigIntリテラルとして動作するが、`(-1n)`、`F(-1n)`、`[-1n]`、`{x:-1n}`のように括弧・配列・辞書内へ直接置くと公式パーサーは構文エラーにする。変数へ入れるか`0n-1n`と書けば使える。一方、変数`A`に対する`-A`は内部的にNumberの`-1`との乗算になり、BigIntとの型混在エラーになる | 区切り内で`-`直後がBigIntならソース位置付きで拒否し、それ以外のリテラルだけ符号を値へ取り込む。変数への単項マイナスは公式と同じ乗算形式を保持して型混在を拒否する | `負のBigIntリテラルと変数への単項マイナスを区別する`、`公式同様に区切り内の負のBigInt直接指定を拒否する`、`parser-diagnostic-cases`、`native-bigint-arithmetic-and-comparison` |
| BigIntとNumberの比較 | 算術演算では型混在エラーだが、`>`などの関係比較と`==`は許可される。`1n==1`は真、`1n===1`は偽 | 比較時だけNumberを精度損失なく整数と照合し、厳格比較では型を区別する | `AOT BigInt比較をNumberとの間でも精度を落とさず処理する`、`native-bigint-arithmetic-and-comparison` |
| `^`と`**` | `^`はXORではなく冪乗である。またJavaScriptの`**`と異なり、両方とも左結合なので`2^3^2`と`2**3**2`は`64`になる | `^`を`**`へ正規化したうえで、両構文を左結合としてNumberとBigIntの冪乗へ下げる | `冪乗演算子を公式同様に左結合として構文解析する`、`native-number-and-bigint-shifts` |
| 負のBigIntシフト量 | `S=-2n`のとき`8n<<S`は右へ2ビット移動した`2`、`8n>>S`は左へ2ビット移動した`32`になる | シフト量の符号で方向を反転し、任意精度のまま処理する | `AOTのNumberとBigIntシフトを公式規則で処理する`、`native-number-and-bigint-shifts` |
| BigIntの`>>>` | 符号なし右シフトはBigIntに定義されず、実行時エラーになる | 値を生成せず`UnsignedShiftOfBigInt`として拒否する | `AOTのNumberとBigIntシフトを公式規則で処理する` |
| `+`と`&`の文字列 | なでしこ式の`+`はJavaScriptの`+`と異なり文字列連結を行わない。`3+「個」`は`NaN`、`1n+「個」`はBigInt型混在エラーになる。連結は`&`を使い、`1n&「個」`は`1個`、`[1,2]&「個」`は`1,2個`、辞書なら`[object Object]個`になる。自己循環した配列は循環箇所を空文字として扱う | インタプリタとAOTの`+`を数値加算に限定し、`&`は循環を検出しながら値をUTF-16文字列へ変換して連結する | `なでしこ式の加算は文字列を連結せず数値へ変換する`、`AOTの値をUTF-16文字列として連結する`、`native-string-concatenation-and-bigint-string-comparison` |
| BigIntと文字列の比較 | 算術では型混在エラーだが、抽象等価・関係比較では整数として解釈可能な文字列をBigIntへ変換する。`1n==「1」`は真、`1n===「1」`は偽 | UTF-16文字列を整数として厳密に解析し、変換不能時は比較を偽にする | `AOT BigInt比較をNumberとの間でも精度を落とさず処理する`、`native-string-concatenation-and-bigint-string-comparison` |
| NaNとInfinityの表示 | Cの`printf`では環境により`nan`や`inf`になるが、公式は`NaN`、`Infinity`、`-Infinity`と大文字小文字を固定して表示する | インタープリタとAOTで共有するJavaScript互換の数値文字列化を使う | `binary64をJavaScript互換の最短文字列へ変換する`、`native-string-concatenation-and-bigint-string-comparison` |
| `増`・`減`の型変換 | 通常の`+`と異なり、未定義の対象は0から開始し、文字列やBigIntもNumberへ変換する。`A=5n; Aを2増`の結果はBigIntではなくNumberの`7`になる | 専用の増減経路で対象と増減量をNumberへ変換し、常にNumberを書き戻す | `増減文は未定義・文字列・BigIntをNumberへ変換する`、`AOT増減は未定義・文字列・BigIntをNumberへ変換する`、`native-increment-and-decrement` |
| 分割宣言の非配列値 | `変数[A,B]=C`でCが非配列でもエラーにせず、C全体を先頭1要素として扱う。Cが`「AB」`なら`A=「AB」, B=undefined`であり、文字単位には分割しない | 配列なら各要素を取得し、非配列なら先頭へ値全体、それ以降へ`undefined`を格納する専用取得処理を使う | `AOT分割宣言は非配列を1要素の値として扱う`、`native-array-destructure` |

## 日時と再現性

日時命令は公式カタログ上で`pure`とされていても、`今`、`今日`、`システム時間`などは実時計を読みます。
lnakoは実行結果の互換性とテストの再現性を両立するため、CLIではホストの実時計を使い、テストでは固定した
壁時計を注入します。日時差分テストは`Asia/Tokyo`で実行し、乱数、タイマー、単調時計もホスト抽象化から
差し替えます。

公式CLIの依存モジュール`shell-quote`は、読み込み時に内部識別子生成のため`Math.random`を4回呼びます。
これはなでしこプログラムの乱数ではないため、固定ホストはコンパイラ内部の乱数と同様に別ストリームへ
分離します。分離しない場合、同じソースでも公式CLI経由とAPI経由で最初の`乱数`が変わります。
差分テストIDは`plugin-math-all`です。AOTランタイムも`LNAKO_TEST_RANDOM_SEED`を受け取り、固定ホストと同じ
xorshift64*系列を使うため、AOT乱数の差分fixtureでは全経路の乱数列を再現できます。未指定時はAOTランタイムが
時刻とプロセス内アドレスから初期状態を作り、通常実行では固定値へ制限しません。

| 境界 | 公式v3.7.24の実際の挙動 | lnakoの扱い | 差分テストID | TODO識別子 |
|---|---|---|---|---|
| 乱数の再現性 | `Math.random`は通常実行では非決定的だが、テストの固定ホストでは`LNAKO_TEST_RANDOM_SEED`から同じ乱数列を生成する。`乱数`は数値・`[先頭,末尾]`配列・辞書の`先頭`/`末尾`を整数範囲へ変換し、`乱数範囲`は2引数の整数範囲を使う | InterpreterとAOTはホスト注入乱数を使い、AOTは同じ固定シード系列をランタイム内で再現する。AOTの未指定シードは時刻とプロセス内アドレスで初期化するため、非決定性を保つ。通常モードへJSランタイムは混入しない | `plugin-math-all`、`native-math-random` | なし |
| AOT日時の固定時計 | `Date`は`TZ=Asia/Tokyo`のローカル時刻を表示し、`Date.now()`はUnix epochのミリ秒を返す。固定ホストでは`LNAKO_TEST_NOW_MS`を注入して`今`・`今日`・年月命令を再現する | AOTは`LNAKO_TEST_NOW_MS`指定時に公式と同じミリ秒を使い、日付境界をAsia/Tokyoへ変換する。未指定時のAOT壁時計はCの秒精度へフォールバックするため、`システム時間ミリ秒`の実時間精度は未実装境界として残る。通常モードへJSランタイムは混入しない | `plugin-system-datetime`、`native-datetime-clock` | `AOT-DATETIME-CLOCK-PRECISION` |
| 日時の書式・元号・差分・加算・単調時計 | 公式は書式トークンを日時の各要素へ展開し、`2019/05/01`を`令和元年05月01日`へ変換する。固定入力では年・月・日・時・分・秒の差分をそれぞれ`4`・`14`・`2`・`3`・`3`・`3`、`日時差(...,"日")`を`7`とし、`時間加算`・`日付加算`・`日時加算`はDateの正規化後にそれぞれ`2024/01/02 00:30:00`・`2024/03/02`・`2024/01/01 01:30:00`を返す。`時間ミリ秒取得`は固定テスト環境の単調時計値を返す | Interpreterと純LLVM AOTは書式トークン、明治以降の元号、JavaScript Date互換の差分・加算・日付正規化を共有し、`LNAKO_TEST_MONOTONIC_MS`で単調時計を再現可能にする。対象経路はInterpreter・純LLVM AOTで、QuickJS互換モードは対象外 | `plugin-system-datetime`、`native-datetime-format-difference-and-addition` | なし |
| `曜日` / `曜日番号取得` | `曜日`はAsia/Tokyoで日曜を0とする`日`〜`土`を返し、無効な日時は公式のNaN経路で`日`になる。`曜日番号取得`は`YYYY/MM/DD`の各部分を接頭辞整数として読み、日曜を0として返す。Dateの範囲外日付はJavaScript Dateと同じく正規化される | Interpreterと純LLVM AOTは同じ日時文字列の正規化、日曜始まりの番号、無効値の扱いを使う | `plugin-system-datetime`、`native-datetime-weekday` | なし |
| `UNIXTIME変換` / `UNIX時間変換` / `日時変換` | `UNIXTIME変換`と別名は日時文字列をAsia/Tokyo基準でUnix秒へ変換し、`日時変換`は数値をUnix秒として`YYYY/MM/DD HH:mm:ss`へ変換する | Interpreterと純LLVM AOTは同じ日時解析、秒単位変換、数値化とAsia/Tokyo表示を使う | `plugin-system-datetime`、`native-datetime-unix-and-format` | なし |
| `秒待` / `秒待機` / `秒逐次待機` / `秒後` / `秒毎` / `秒タイマー開始時` / `タイマー停止` / `全タイマー停止` | 公式は待機時間をNumberへ変換して秒からミリ秒へ切り捨て、0以下・非有限値は待たずに完了する。単発・周期タイマーは登録順に同時刻イベントを処理し、`対象`へタイマーIDを設定してコールバックへIDを渡す。周期タイマーはコールバック前に再登録されるためコールバック内で停止でき、全停止は保留中のタイマーを取り消す。公式CLIはトップレベルの待機・コールバックを処理するが、公式生成JavaScriptはトップレベルの非同期待機を完了せず、終了成功・空出力になる | Interpreterは決定的な仮想時計とイベントループでPromise・タイマーを処理する。純LLVM AOTはJS runtimeを使わずRuntime内のキューへコールバック値を保持し、実時間をブロックして同時刻の登録順に処理する。生成main終了前に保留イベントをドレインし、コールバック例外は既存のAOT例外経路へ渡す。通常モードへJavaScript runtimeは混入しない。QuickJS互換モードはこのAOTタイマー境界の対象外とする | `native-system-timer-wait`、`native-system-timers` | なし |
| `元号データ`の順序と共有値 | 公式は初期化時に令和・平成・昭和・大正・明治の順で5件を作り、各辞書へ`元号`、`改元日`をこの順で格納する。`改元日`は`YYYY/MM/DD`の文字列で、同じグローバル定数を参照する | Interpreterと純LLVM AOTは、公式CLI source/generated JavaScriptとのfixture比較と同じ5件の配列・辞書・property順を使う。AOTは起動時に生成し、グローバルとRuntimeキャッシュのidentityを保持する。カタログ上は`plugin_system`と`plugin_datetime`に同名entryがあるため、命令名だけではcatalog IDを分離できず、実行証拠は`unverified`のまま扱う | `plugin-system-datetime`、`native-datetime-era-data` | なし |

## マークアップと漢数字

| 命令 | 公式v3.7.24の実際の挙動 | lnakoの扱い | 差分テストID |
|---|---|---|---|
| `HTML整形`の空行 | `html` 1.0.0のトークン単位出力をそのまま返すため、`head`や`body`の前に挿入する空行にも現在のインデント空白が残る | Interpreterと純LLVM AOTで2空白インデントだけでなく、空行上の空白も同じバイト列で返す | `plugin-markup-all`、`native-markup-commands` |
| `マークダウンHTML変換`の末尾改行 | `marked` 18.0.7は見出し・段落・リストなどの生成HTMLに末尾改行を付けるが、HTMLブロックは入力の改行をそのまま保ち自動追加しない | Interpreterと純LLVM AOTでブロック種別ごとに同じ末尾規則を使う | `plugin-markup-all`、`native-markup-commands` |
| `漢数字` | 小数部の0は`〇`、整数値0は`零`。全角数字と指数表記を先に10進表記へ展開し、先頭の`+` / `-`は漢数字にせず保持する。公式実測では`漢数字(10001)`が`一万一`、`漢数字(0.01)`が`零・〇一`、`漢数字("1e-3")`が`零・〇〇一`になる。公式生成JavaScriptのstandaloneは取り込んだpluginを登録せず実行時エラーになるため、このAOT fixtureのoracleは公式CLIに固定する | Interpreterと純LLVM AOTで同じ前処理、零の字種、ASCII符号を使う。AOTはUTF-16入力をBigInt境界まで処理する。対象経路は公式CLI・公式生成JavaScript・`lnako run`・AOT O0〜O3、QuickJSは対象外。TODO: なし | `plugin-kansuji-all`、`native-kansuji-commands` |
| `漢数字`のNumber判定後の変換 | 空文字はJavaScriptの`Number("")`が0なので`零`になる。`.5`、`1.`も受理する。一方、`Infinity`、`0x10`、前後空白もNumber判定は通るが、変換本体は元の文字列を4桁単位で処理し、数字以外を文字列`undefined`へ変換する | InterpreterはNumber判定と、その後に続く文字単位変換の不整合まで再現する。AOTはこの生成境界をまだ公式7経路fixtureへ含めていないため、全境界を実装済みとは扱わない | `公式CLI・公式生成JavaScript・Interpreter` | `TODO: aot-kansuji-generated-boundaries` |
| `漢数字`の指数展開 | 正数の指数表記だけを独自展開する。整数部が2桁以上の`10e1`や`01e2`は通常の科学表記より0を1桁多く補って`千`になり、`.5e1`は末尾の小数点を残して`五・`になる。先頭に符号がある`-1e3`は展開せず、`e`を`undefined`として変換する | Interpreterは正規表現の適用範囲と小数点移動式の境界誤差を含めて再現する。AOTの標準的な指数入力`1e-3`は公式7経路で確認済みだが、生成境界全体は未検証として残す | `公式CLI・公式生成JavaScript・Interpreter`、`native-kansuji-commands` | `TODO: aot-kansuji-generated-boundaries` |
| `算用数字`の非標準表記 | 連続する基本漢数字は10進の桁列ではなく、内部の2要素組を乗算する。そのため`一二万`は20,000、`一二`は0になる | 一般的な漢数字に加え、この組化アルゴリズムもInterpreterと純LLVM AOTで再現する。対象経路はInterpreter・公式7経路AOT、QuickJSは対象外。TODO: なし | `plugin-kansuji-all`、`native-kansuji-commands` |
| `算用数字`の大単位 | `万`や`無量大数`のように係数を書かない大単位は1倍ではなく0を返す。`一無量大数`はBigIntを返す | 係数の既定値とNumber/BigInt境界をInterpreterと純LLVM AOTで同じにする。`一無量大数`は10^68のBigIntとして保持する。対象経路はInterpreter・公式7経路AOT、QuickJSは対象外。TODO: なし | `plugin-kansuji-all`、`native-kansuji-commands` |

## Caniuse標準プラグイン

公式v3.7.24の`plugin_caniuse.mjs`は、ビルド時に生成した`browsers.mjs`をモジュールの共有オブジェクトとして返します。
このデータは実行時にネットワークやJavaScriptを読み込むものではなく、固定した公式oracleの生成物に含まれるスナップショットです。

| 境界 | 公式v3.7.24の実際の挙動 | lnakoの扱い | 対象経路 | 差分テストID | TODO識別子 |
|---|---|---|---|---|---|
| `対応ブラウザ一覧取得`のデータとidentity | `対応ブラウザ一覧取得()`は`browsers.mjs`の16キー・各バージョン配列を返し、同じプラグイン状態では呼出しごとに同じオブジェクトを返す。キーは生成ファイルの順序で列挙される。公式`--compile`のstandalone JavaScriptはimportしたcaniuse pluginを登録しないため、このfixtureでは公式CLI実行をoracleにする | InterpreterはZig製caniuse plugin state、AOTはv3.7.24生成データを純Zigで構築したランタイムキャッシュを返す。AOTは通常モードへJavaScriptを混入させず、同一Runtime内の呼出しidentityを保持する | 公式CLI source、Interpreter/AOT。公式生成JavaScriptとQuickJSは対象外 | `plugin-caniuse-all`、`native-caniuse-browsers` | なし（公式生成経路の制約） |
| `ブラウザ名変換表`のデータとidentity | 公式は`browsers_agents.mjs`の19キー辞書を定数として公開し、同じimport済みオブジェクトを参照する | InterpreterはZig製caniuse plugin state、AOTは公式生成データをUTF-16キー・値の純Zig挿入順辞書として構築し、グローバル定数とRuntimeキャッシュのidentityを保持する。通常モードへJavaScriptを混入させない | 公式CLI source、Interpreter/AOT。公式生成JavaScriptとQuickJSは対象外 | `plugin-caniuse-all`、`native-caniuse-agents` | なし（公式生成経路の制約） |

## モジュール取り込み

| 構文 | 公式v3.7.24の実際の挙動 | lnakoの扱い | 差分テストID |
|---|---|---|---|
| Windowsの絶対パスを使う`取り込む` | `D:/path/plugin.mjs`のようなドライブ文字付きパスも相対モジュール名として扱い、現在のソースディレクトリを先頭に付ける。このため、存在する絶対パスでも取り込みに失敗する | 利用者の絶対パスは正規化して読み込む。公式オラクル用fixtureは同一ドライブ上に置き、相対パスで取り込んでOS間の比較条件を揃える | `plugin-markup-all`、`plugin-kansuji-all`、`plugin-caniuse-all` |

## QuickJS互換モード

| 命令・API | 公式v3.7.24の実際の挙動 | lnakoの扱い | 差分テストID |
|---|---|---|---|
| `JSオブジェクト取得` / `sys.__findVar` | Mapにキーが存在するかではなく取得値をtruthy判定する。そのため値が`0`、`false`、空文字列、`null`なら、第2引数の既定値または`null`を返す | 値の型を保持したまま同じtruthy探索を再現する | `compat-js-find-quirks` |
| `sys.__getSysVar`と`sys.__findVar` | `__getSysVar("X")`はシステム変数の非修飾キーだけを調べる。一方`__findVar("X")`はローカル変数と`module__X`を探索するため、同じ名前でも結果が異なる | システム大域表と現在フレーム・モジュール探索を分離する | `compat-js-host` |
| JS構文・呼び出しエラーのCLI終了 | 公式`cnako3`はSyntaxError等をstderrへ出しても終了コード0になる経路がある | lnakoは診断をstderrへ出し、実行失敗を終了コード1で返す。差分テストは双方で失敗が観測できることを検証する | `compat-js-error-eval`、`compat-js-error-function`、`compat-js-error-method` |
| `build --compat-js`の`--emit` | 公式処理系には対応するネイティブ埋め込み出力区分がない | QuickJS対応ランタイムを含む自己完結bundleなので`exe`だけを許可し、`obj` / `llvm-ir`は曖昧な中間生成物を出さず拒否する | `compat-js-plugin-imports` |
| QuickJS本体のReleaseSafe検査 | 上流Cはtagged pointer、callback cast、flexible array、符号付きshift等の低水準表現を使い、Debugでは動いてもZigのC UBSanがmacOS / Windowsでtrapする | ハッシュ固定した上流QuickJS Cだけ`undefined`群と`function`検査を外す。自作bridgeとZigコードはReleaseSafe検査を維持し、3環境で実行テストする | `Differential QuickJS compatibility test`、`Smoke test` |

## ネイティブプラグイン

| 境界 | 分かりにくい挙動 | lnakoの扱い | テストID |
|---|---|---|---|
| `.dylib` / `.so` / `.dll`の`取り込む` | 公式v3.7.24には対応するネイティブABIがなく、JSプラグインとはロード・所有権・スレッド境界が異なる | ソースとして読まず正規化パスをIRへ保持し、`run` / `test`の初期化時に`lnako_plugin_v1`を検証してロードする | `native-plugin-abi` |
| `check`時のネイティブライブラリ | ABI初期化は任意のネイティブコードを実行するため、静的検査との境界が分かりにくい | `check`は拡張子・取り込み構造・動的命令構文までを検査し、ライブラリのロードとABI検証は行わない。欠落・ABI不一致は`run` / `test`で診断する | `native-plugin-open-failure`、`native-plugin-abi-mismatch` |
| ネイティブプラグインを含む`build` | IR実行器では利用できるが、LLVM AOTランタイムにはまだ動的ABI dispatcherがない | 不完全な実行ファイルを生成せず、終了コード2と専用診断で拒否する | `native-plugin-abi-aot-rejection` |
| 厳格モードの外部命令名 | 命令一覧は動的ライブラリの初期化時に登録されるため、意味解析時には確定していない | ネイティブプラグインを直接取り込むモジュールでは未知の「命令」だけを動的解決対象にする。未知の変数は従来どおり診断し、未登録命令は実行時に拒否する | `native-plugin-strict-dynamic-command` |
| 標準命令と同名の登録 | `表示`等は実行器内の専用経路とプラグイン経路で優先順位が異なり、上書きを許すと命令ごとに挙動が変わる | ネイティブ同士の重複に加え、固定した標準cnako命令527件との名前衝突も登録時に拒否する | `ネイティブ命令登録の属性と重複を検証する` |
| opaque値のUTF-8参照 | 任意値を暗黙に文字列化すると、配列・辞書の変更後にキャッシュ済みポインタが古くなる | `get_utf8`は不変なString / BigIntだけを受け付け、他の型は型別getterで明示的に取得する。ポインタはhandle解放まで有効 | `native-plugin-abi` |
| 非同期完了と終了処理 | workerから呼べる関数とtokenの有効期間が通常のhost callbackと異なる | 別スレッドで許可するのは`complete_async`だけ。終了時は`deinitialize`でworkerを停止した後に未完了tokenと命令contextを破棄する | `native-plugin-async-thread` |
| 非WindowsホストからのMSVCクロスビルド | Zig 0.16.0は`x86_64-windows-msvc`向けlibcを非Windows環境へ同梱しないため、C fixtureをリンクできない | ローカルの構文・ABIクロス検査は`x86_64-windows-gnu`で行い、正式なWindows x86_64 MSVCのビルド・実ロード・実行は`windows-2025` CIランナーで検証する | GitHub Actions `Windows x86_64` / `Native plugin ABI test` |

ABI値の所有権と非同期スレッド制約は[`NATIVE_PLUGIN_ABI.md`](NATIVE_PLUGIN_ABI.md)へ分離して記録します。

## 最適化レベル

| 境界 | 分かりにくい挙動 | lnakoの扱い | テストID |
|---|---|---|---|
| `-O0`と`-O1`以上 | Nako SSA最適化とLLVM PassBuilderは別段階であり、単にLLVMの最適化レベルだけが変わるわけではない | `-O0`は元IRと動的変換を維持する。`-O1`以上は独立複製したIRへ型推論・定数伝播・直接呼び出し・DCEを適用し、検証後にLLVMへ渡す | `LLVM C APIで全最適化レベルのモジュールを検証してIRを出力する`、`Differential native AOT test` |
| 関数引数の型推論 | ある静的呼び出しが数値だけを渡していても、同じ関数が関数値や再帰経路から別の型で呼ばれる可能性がある | 関数値として外へ渡る関数は狭めない。再帰を含む全静的呼び出しの実引数型が一致した場合だけ狭める | `関数値として外部へ渡る関数の引数型を狭めない`、`異なる型を渡す再帰呼び出しでは引数型を狭めない` |
| 数値アンボックス | タグ付き値のpayloadを直接`double`として使う処理は、型推論が誤ると動的変換より高速でも意味が変わる | `-O1`以上かつ`number`と証明された演算入力だけ直接取り出す。真偽判定も`number` / `boolean`と証明された場合だけ専用LLVM命令へ下げる | `O1では証明済み数値と真偽判定をアンボックスしO0のIRを変更しない` |
| NaNと符号付きゼロの定数伝播 | `NaN == NaN`は偽だが、最適化器内部では同じNaN事実が収束したかを判定する必要がある。また`+0`と`-0`は除算などで区別できる | 言語の比較演算はJS規則を使い、最適化器の事実同一性だけをbinary64のbit列で判定する。剰余ゼロはNaN、`-0`の剰余は符号を保持する | `定数畳み込みでNaNと符号付きゼロを区別する` |
| 定数分岐先のphi | 条件が定数でも、捨てる辺がphiの唯一の入力なら、辺だけを消すとLLVM IRとして不正な0入力phiになる | その形の分岐は保守的に維持し、後段LLVM最適化へ任せる | `唯一の入力を持つphiへの定数分岐は保守的に維持する` |

全最適化レベルの意味同一性は、公式cnako3、公式生成JavaScript、`lnako run`、AOT `-O0` / `-O1` /
`-O2` / `-O3`の7経路差分テストで確認します。

## AOTランタイムとビルド検証

| 境界 | 分かりにくい挙動 | lnakoの扱い | テストID |
|---|---|---|---|
| AOTの`-O0`〜`-O3`とランタイム静的ライブラリ | CLIの最適化指定はNako SSA IRと生成LLVMモジュールに対する指定であり、別にリンクするZigランタイムまで同じモードにするとDebug版だけがZigの診断用OS依存を持つ | 生成コードは指定レベルを維持し、配布ランタイムはReleaseSafe・strip・compiler-rt同梱で固定する。WindowsではZigの安全検査経路が使うNT APIを`ntdll.lib`で解決する | `Differential native AOT test`、GitHub Actions `Windows x86_64` |
| 16バイト動的値のWindows ABI | `%lnako.Value`をC関数の引数・戻り値として値渡しすると、Windows x64とSystem Vで集約値の呼出規約が異なる。macOS/Linuxで動いてもWindows生成物だけで配列生成時のアクセス違反や、関数コールバックの戻り値欠落が起こり得る | Zig製AOTランタイムとの公開境界では動的値をすべてポインタ渡しにする。LLVM生成コールバックも明示的な結果ポインタへ書き込み、集約値のC ABI分類へ依存しない | `公開AOT ABIは動的値をポインタで受け渡す`、`native-noncapturing-function-value`、`native-dynamic-function-arity`、`native-closure-shared-mutable-binding`、`native-closure-transitive-capture`、GitHub Actions `Windows x86_64 / aot` |
| binary64の表示桁数 | Cの`printf("%.17g")`は値の往復に十分でも、`PI`を`3.1415926535897931`と表示し、JavaScriptの最短表記`3.141592653589793`と一致しない | インタープリタとAOTで同じ最短表記変換を共有し、`NaN`・無限大・指数表記境界・負のゼロもJavaScript規則へ合わせる | `binary64をJavaScript互換の最短文字列へ変換する`、`native-scalar-system-constants` |
| `check_compat_report.mjs`をQuickJSビルド後に実行 | 検査スクリプト自身が通常版を再ビルドすると、直前に生成したQuickJS対応`lnako`を同じ`zig-out/bin`上で置き換える | 単独実行は従来どおりビルドしてから検査し、既存配布物を検査するCIでは`--no-build`を指定してバイナリを変更しない | `check_compat_report.mjs --no-build`、GitHub Actions `Smoke test` |
| 同名命令のcatalog IDと実装route | 標準527 entryには同名異pluginが31組あり、命令名だけのfixtureはどのcatalog IDを実行したか証明しない。特に`ファイル名抽出`・`パス抽出`はNode host有効時に`plugin_node`が`plugin_system`より先に選ばれる一方、datetime別名は共通実装へ合流する | catalog identityとimplementation routeを分離する。一意名だけ自動解決し、同名は公式の探索順と実traceで一意に確定できる場合だけIDへ結び付ける。現時点で追跡中のdispatch証拠を公式差分まで結合して`trace-confirmed-unattested`にできるのは、`native-cut-commands`の一意名4命令（`切取`・`範囲切取`・`CHR`・`表示`）だけであり、attestationを含まない追跡JSONの`verified`は0件。Node側2命令・system側・datetime別名は`unverified`のままにする | `check_dispatch_trace.mjs`、`dispatch-evidence.json`、`native-cut-commands`、`plugin-node-path-host` |
| AOT compile manifestの証明範囲 | 最適化前IRにbuiltin callがあること、LLVM最適化後もcallが残ること、実行時に成功することは別の事実である。生成途中で止まったJSONLも完了artifactにはできない | `LNAKO_COMPILE_MANIFEST`はNako最適化前の命令名・numeric opcode・canonical opcode・LLVM emitter route・決定的な16桁site ID・位置を記録し、link成功後の`complete` recordがある場合だけ受理する。site ID衝突はbuildエラーとする。pluginやcatalog IDはmanifest自身へ持たせずchecker側で限定解決する。失敗時は部分ファイルを削除し、runtime traceと公式差分結果を別途照合する | `check_dispatch_trace.mjs`、`lnako.aot.builtin-manifest.v1` |
| dispatch evidenceのattestation境界 | JSONのdigestだけではGitHub Actionsの実行者や対象commitを証明できず、未署名の単体ファイルを`verified`へ上げると差し替えを検出できない。署名検証済みという文字列を持つmetadataだけでも偽造できる。fork PRではOIDC／attest権限も与えられない | AOT各正式OSのdispatch JSONを公式`actions/attest`のmulti-subject artifact attestationへ結び付け、専用jobとcatalog同期処理の双方で、生成されたSigstore bundleを公式`gh attestation verify`へ渡す。`--signer-workflow`・`--signer-digest`・`--source-digest`・`--source-ref`・公式OIDC issuer・`--deny-self-hosted-runners`を強制し、bundle SHA-256、3 OSの証拠digest、commit、workflow identityがすべて一致した場合だけ一時catalog evidenceを`verified`として生成する。追跡JSONはnull attestationのままにし、未attest、metadataだけ、権限不足、digest／identity不一致は昇格しない。Actions artifactの30日保持後も検証できるよう、run `32983175945`の3 OS JSON、bundle、metadata、historical catalogを`compat/v3.7.24/attestations/32983175945/`へ固定し、manifestとbundle payloadのidentity／digestをcheckerで再検査する。target commitと追跡を追加する後続commitは別で、現在の`evidence.json`はverified 0のまま | `tools/verify_dispatch_attestation.mjs`、`tools/sync_compat_evidence.mjs`、`tools/check_tracked_dispatch_attestation.mjs`、GitHub Actions `actions/attest@v4.2.2`（SHA固定）、`gh attestation verify` |
| 追跡bundle改変テストの診断範囲 | 改変後の`gh attestation verify`が非成功になることはfail-closedを確認するが、テスト単体では署名不一致と`gh`未導入・認証・通信障害を区別しない | mainのattestation jobで正規bundleのオンライン検証を先に独立して成功させる。改変テストのログは暗号学的拒否の単独証明とは表現せず、正規bundle検証と組み合わせた回帰防止として扱う | `tools/check_tracked_dispatch_attestation.mjs`、`tools/check_tracked_dispatch_attestation_security.mjs`、GitHub Actions `attest-dispatch-evidence` |
| Windowsのdispatch証拠と改行 | Windowsでは公式cnako3・生成JavaScript・InterpreterがLFを出しても、MSVCリンクしたAOT実行ファイルの標準出力はCRLFになる。生bytes比較では同じ表示結果を不一致にし、Windows証拠だけ生成できない | 全159件のAOT差分と同じく、公式比較に使うstdout/stderrはCRLFをLFへ正規化してから比較・SHA-256化する。trace有無の同一実行比較とraw trace／manifest hashは生bytesのまま保持する | `tools/check_dispatch_trace.mjs`、`tools/compare_native_oracle.mjs`、Windows x86_64 `aot` |
| dispatch evidenceのraw hashと意味内容 | `compileManifestSha256`はraw manifest headerに含まれる一時`sourcePath`もhashするため、同じ意味内容でも実行ごとに変わり得るrun固有provenanceである | 証拠JSON自体にはsourcePath本文を保存せず、site・opcode・canonical opcode・routeなどの意味内容を再生成して一致確認する。今回の再生成ではraw hash以外の証拠内容が完全一致し、Interpreter/AOT trace hashも一致した | `dispatch-evidence.json`、`check_dispatch_trace.mjs`、`sync_compat_evidence.mjs` |
| AOT差分artifactの完全allowlist | schema、hash、route、fixture、compile status/manifest要約まで全object階層の既知keyを検査し、stdout/stderr・引数・値・ポインタ・sourcePath・cwdとネストされたenvironment、未知key、未知status/route値を拒否する。トップレベルのprovenance environmentは正規フィールドとして維持する | 生成側の`lnako.native-oracle-artifact.v1`形状とvalidatorを同じfeature単位で更新し、schema変更時はallowlistを同時移行する | `tools/compare_native_oracle.mjs`、`DEVELOPMENT.md` |
| 公式oracle tree hashのmodeとsymlink | ファイルmodeはNode CLIが読むJS内容の意味に影響しない一方、OSごとに異なるためhash対象外。production treeにsymlinkが残ると外部ファイル参照の穴になり得る | stagingのproduction prune後に全階層`node_modules/.bin`と`.package-lock.json`を明示削除し、tree hashはmarker以外を全てhashしてsymlinkを拒否する。mode・mtime・絶対パスは記録しない | `tools/setup_oracle.mjs`、`tools/oracle_tree_hash.mjs`、`compat/upstream.lock.json` |
| AOT関数値の文字列化 | JavaScriptの関数文字列には関数名と公式経路では関数本体のソースが含まれ、名前中の数字は`通貨形式`や文字種判定の結果にも影響し得る | Interpreterは内部wrapper名を、AOTは生成ABIで渡されたwrapper名を保持し、`function <name>() { [native code] }`へUTF-16変換する。AOTとInterpreterは公式CLI／生成JavaScriptのソース本文までは保持しないため、完全な`Function.prototype.toString`互換を`TODO: aot-function-string-name`として未実装扱いにする。対象経路はInterpreter/AOT、QuickJSは対象外 | `AOT関数値の文字列化は生成ABIの関数名を保持する`、`TODO: aot-function-string-name` |

## 礼節・言語カタログ・名前空間

| 命令・境界 | 公式v3.7.24の実際の挙動 | lnakoの扱い | 差分テストID |
|---|---|---|---|
| 初回の`敬具` | 内部値が未定義のまま`100`を加えるため一度`NaN`になり、直後の`礼節レベル取得`がfalsey値として`0`へ戻す | InterpreterとAOTで初期状態を未定義相当で保持し、同じく取得結果を`0`にする | `system-runtime-courtesy`、`native-system-courtesy` |
| `敬具`の加点 | `拝啓`や取得済みの初期値で内部値が`0`になった後は、`100`を加点する | InterpreterとAOTで同じ | `system-runtime-courtesy`、`native-system-courtesy` |
| 標準出力のプールと履歴 | `継続表示`と`連続無改行表示`は直ちに出力せずプールへ加え、`連続表示`と`表示`がプールを先頭へ連結して改行出力し、`表示ログ`へ出力行を追記する。`言`と`コンソール表示`は全引数を連結して改行するが履歴へ追記しない | InterpreterとAOTで同じプール・表示ログ・全引数出力規則を処理する。AOTの`表示`/`連続表示`はdirect-display、残りは`stdio_*` ABIで実行する | `plugin-system-stdio`、`native-system-stdio` |
| 初期`名前空間` | 公式CLI直接実行では`main`固定でなく入力ファイルのベース名。末尾が`.nako` / `.nako3`のときだけ拡張子を外し、ハイフンや別のピリオドは残す。一方、公式生成JavaScript単体では初期化経路が異なり空文字のままになる | `lnako run`とソースから作るAOTは公式CLI側に合わせ、同じファイル名をIRのモジュール名、関数の修飾名、初期`名前空間`に使う。AOT差分ケースは`oracle: official-source`を明示する | `system-runtime-catalog-and-namespace`、`公式と同じファイル名をモジュール名に保つ`、`native-string-system-constants` |
| `プラグイン名設定` / `名前空間設定` / `名前空間ポップ` | `プラグイン名設定`と`名前空間設定`は最後の引数を文字列化して設定する。`名前空間設定`時点の`名前空間`だけでなく`プラグイン名`も対で保存し、後から変更された両方を`名前空間ポップ`で復元する。空スタックのポップは何もしない | InterpreterとAOTで同じ文字列化境界とスタック復元を処理する。AOTは専用ABIで対象globalを明示し、スタック内の2値をGCルートとして追跡する | `system-runtime-catalog-and-namespace`、`native-system-plugin-management` |
| `ASYNC` | 公式の同期実行では値を返さず、後続の非同期処理を構成するためのno-opとして完了する | InterpreterとAOTでundefined相当を返す。Promiseを待機する`AWAIT実行`はこのno-opへ暗黙に縮退させない | `system-runtime-execution-and-debug`、`native-system-async` |
| `実行` / 同期の`AWAIT実行` | `実行`は関数値または関数名を引数なしで呼び、関数でない値はそのまま返す。`AWAIT実行`は第2引数を配列なら展開し、Promiseでなければ戻り値をそのまま返す | Interpreterと純LLVM AOTで同じ関数値・名前解決・配列展開を処理する。AOTはcore Promiseの実行ループで保留Promiseを待機し、`fulfilled`値を返し、拒否理由を保留例外として伝播する。`ナデシコ`による動的ソース実行は別経路で、通常AOTへJSを混入させない | `system-runtime-execution-and-debug`、`native-system-execute`、`native-system-promise-timer-await` |
| `ナデシコ` / `ナデシコ続`の公式経路差 | 公式cnako3の直接CLIはfixture `native-system-dynamic-execution`で終了コード0、stdoutが`NaN\n1\n1\n\n2\n`になる。公式生成JavaScript単体は同じソースで終了コード1、stdout空になる | Interpreterと純LLVM AOT O0〜O3は公式直接CLIの出力に一致する。AOTは文字列ソースを埋め込みZig Interpreterで構文解析・意味解析・SSA検証・実行し、グローバルと`表示ログ`を同期する。これは実装済みの意図的な経路選択であり未実装ではない。対象はInterpreter/AOT、QuickJSは対象外。公式生成JavaScriptとの差は生成standalone routeの既知の制限として扱い、TODO: なし | `native-system-dynamic-execution`、`dynamic-nadesiko`、`dynamic-nadesiko-scope`、`dynamic-nadesiko-result`、`dynamic-nadesiko-continue`、`system-runtime-execution-and-debug` |
| `実行時間計測` | 公式は文字列を`sys.__findFunc`で関数へ解決し、関数値・解決した関数を`sys`付きで実行して、`performance.now()`（非対応時は`Date.now()`）の差をミリ秒で返す | Interpreterはhostの単調時計で関数名・関数値を解決して実行する。純LLVM AOTは`system_measure_time`でnamed functionまたは関数値を解決し、同じAOT単調時計を前後で読む。固定テスト時計では公式・Interpreter・AOTとも0を返す。QuickJSはこの標準命令の証拠対象外 | `system-runtime-execution-and-debug`、`native-system-measure-time` | なし |
| `グローバル関数一覧取得` | コンパイル済みのグローバル関数名を生成順で返し、モジュール入口と無名lambdaは含めない。モジュール関数名は`モジュール名__関数名`の修飾名になる | InterpreterはIRの関数列、AOTは起動時に登録したnamed function列を同じ順序で列挙する。AOTの関数登録前にこの命令を実行する経路は存在しない | `system-runtime-catalog-and-namespace`、`native-system-global-function-names` |
| `システム関数一覧取得` | 名前に反して関数だけでなく定数も含む。標準527命令そのものでもなく、既定7プラグインを登録したMapの重複を除いた順序に、カタログ外の別名も含めた478件を返す | 固定カタログから公式のプラグイン登録順・重複上書き・別名を再現した478件を生成する | `system-runtime-catalog-and-namespace`、`native-system-catalog-lists` |
| `システム関数存在` | 最後の引数を文字列化して既定システム名のMapだけを調べ、登録名なら`true`、未知名や引数なしなら`false`を返す | AOTは最後の引数をUTF-16からUTF-8へ変換し、同じ固定カタログだけを照合する | `system-runtime-catalog-and-namespace`、`native-system-catalog-lists` |
| HTTP/AJAXの初期グローバル | 公式CLI・生成JavaScriptではNodeの`AJAXオプション`は空文字。HTTPサーバープラグイン未取り込みの`HTTPメソッド`・`GETデータ`・`POSTデータ`・`FILESデータ`は`null`で、取り込み後にサーバー処理が値を設定する | Interpreterは`plugin_httpserver`を明示取り込みした場合だけ4変数を空文字で登録し、通常の`lnako run`では未登録値を保つ。AOTも未取り込み状態の5件を共有システム定数として生成し、取り込み後はネイティブHTTP server stateへ接続する。初期値は`native-node-http-initial-constants`、実通信は`plugin-httpserver-all`で公式CLI・Interpreter・純LLVM AOT O0〜O3を比較する。QuickJSは対象外。TODO: なし | `native-node-http-initial-constants`、`plugin-httpserver-all` |
| `プラグイン一覧取得` / `モジュール一覧取得` | 追加取り込みのないCLIでは、どちらも既定7プラグインの同じ配列を返す | 同じ配列と登録順を返す | `system-runtime-catalog-and-namespace`、`native-system-catalog-lists` |
| `助詞一覧取得` | 最長一致用の内部表ではなく公開用48件を返し、`ものを`などの字句解析用派生形は含めない | 字句解析表と公開表を分離する | `system-runtime-catalog-and-namespace`、`native-system-catalog-lists` |
| `予約語一覧取得` | 字句解析で使う全派生表ではなく、公式が公開する予約語38件を固定順序で返す | Lexer内部の予約語判定表と公開用配列を分離し、同じ38件をAOTで生成する | `system-runtime-catalog-and-namespace`、`native-system-catalog-lists` |
| `AWAIT実行`の引数 | 第2引数が配列でなければ、値を1要素配列へ包んでから関数へ展開する。関数の戻り値がPromiseでなくてもその値を返す | 同じ引数展開を行い、Promiseの場合だけホストイベントループを完了まで進める | `system-runtime-execution-and-debug` |
| `ハテナ関数設定`の配列 | 指定した関数を左から順に適用するが、`ハテナ関数実行`自体は最後の結果を返さず常に`undefined`。`JS:`で始まる要素はJavaScriptを評価する | 非JSの命令名を左から順に呼び、戻り値を次のcallbackへ渡す。`JS:`要素だけは通常モードで拒否し、`--compat-js`を要求する | `system-runtime-execution-and-debug`、`native-system-hatena-custom`、`compat-js-hatena` |
| `ハテナ関数設定`の文字列 | 非修飾のユーザー関数名を探索せず、システム変数表だけから取得する。ユーザー関数を指定するには関数値を渡す必要がある | 文字列はシステム命令名としてだけ呼び、ユーザー関数は関数値の場合だけ受け付ける | `system-runtime-execution-and-debug`、`native-system-hatena-custom`、`system-runtime-hatena-user-name-error` |
| `ハテナ関数実行`／`??`の既定動作 | `ハテナ関数設定`を先に呼ばなければ、`デバッグ表示`相当として入力ファイル名・行番号・値を出力し、命令自体は値を返さない | InterpreterとAOTは同じ既定動作を行う。AOTは設定済みの非JS callback列なら各戻り値を次へ渡し、命令自体は`undefined`を返す。`JS:`評価は通常AOTへ持ち込まない | `native-system-hatena-default`、`native-system-hatena-custom` |
| `デバッグ表示` | `__line`の0始まり行番号へ1を加え、入力時のパスを含む`ファイル(行): 値`を表示する。配列・辞書などは先にJSON化する。Windowsではドライブ文字直後のコロンでパスを分割するため、`C:\\...`が`C(行): 値`になる | IRへモジュール名と入力パスを保持し、同じ行番号・JSON化規則・Windowsのドライブ名短縮を再現する | `system-runtime-execution-and-debug`、`native-system-debug-display` |
| `__DEBUG`の診断ダンプ | デバッグ有効化に加え、Nodeの`console.log`でNakoGlobal内部全体を出す。生成JavaScript全文、循環参照、Mapなどを含み、Nodeの検査表示に依存する | デバッグ状態は有効化するが、製品内部表現の巨大ダンプは標準出力へ出さない。AOTも内部フラグを有効化し、公式側の診断だけはoracle用hostで言語出力から分離して比較する | `system-runtime-execution-and-debug`、`native-system-debug` |
| `__DEBUG_BP_WAIT` | ブレイクポイント一覧または`__DEBUG強制待機`が真なら、`プラグイン名`が`メイン`のとき`@__DEBUG_BP_WAIT(行番号)`を出力して`__DEBUG待機フラグ`が1になるまで500ms間隔で待機し、それ以外のプラグインでは未解決Promiseを返す。該当しなければ行番号を返す | Interpreterと純LLVM AOTは4つのシステムglobalを共有し、AOTは専用`debug-breakpoint-wait` ABIで同じ分岐と待機を処理する。専用fixtureは非該当経路を公式CLI・生成JavaScript・Interpreter・LLVM O0〜O3で比較する。QuickJSは対象外 | `system-runtime-execution-and-debug`、`native-system-debug-breakpoint-wait` |
| `__DEBUG_BP_WAIT`の非メインプラグイン | ブレイクポイントに一致しても`プラグイン名`が`メイン`でなければPromiseをresolveせず、その後の処理が進まない | 同じく未解決Promiseを返す。差分テストでは非該当の即時復帰と、待機フラグを事前設定した該当経路を検証する | `system-runtime-execution-and-debug` |
| `ASSERT等`の成功時 | Nodeの`assert.strictEqual`を呼ぶため、比較成功時の戻り値は`true`でなく`undefined`。`NaN`同士は一致し、`+0`と`-0`は不一致になる | Interpreterと純LLVM AOTでSameValue相当の比較を行い、成功時は`undefined`を返す | `system-runtime-execution-and-debug`、`native-node-assert-equal` |

## BASE64とパス操作

| 命令 | 公式v3.7.24の実際の挙動 | lnakoの扱い | 差分テストID |
|---|---|---|---|
| `URLエンコード` / `URLデコード` / `URLパラメータ解析` | `URLエンコード`と`URLデコード`はUTF-16の文字列をURI component規則で変換し、`URLパラメータ解析`は`?`以降を`&`と`=`で分解して値を復号する。同じキーは後の値で上書きされる | Interpreterと純LLVM AOTは同じUTF-16・パーセント列・重複キー末尾優先の規則を使う。AOTのパラメータ解析は文字列を受け、URLエンコード/デコードは公式の文字列化境界を使う | `plugin-system-url`、`native-system-url-and-base64` |
| `BASE64エンコード` / `BASE64デコード` | エンコードはNodeの`Buffer.from(text).toString('base64')`規則で文字列・Buffer・Uint8Array・ArrayBufferをバイト列として扱う。数値配列を渡すlnako固有の利用も、各要素の整数化・256剰余を経て同じ結果になる。デコードは`Buffer.from(S, "base64")`規則により、空白などの非BASE64文字を無視し、URL-safeの`-` / `_`とpadding省略も受理する。復号バイトが不正UTF-8でも例外にせず`U+FFFD`へ置換する | Interpreterと純LLVM AOTは同じ寛容な字句規則とWHATWG互換UTF-8置換を使う。AOTはUTF-16文字列・数値配列・`byte_buffer`（Buffer/Uint8Array/ArrayBuffer）のエンコード、および文字列のデコードを対象とする。`表列挿入`のBuffer.slice共有はfixtureで検証済み。Bufferの継承property、その他のslice/view identityなど、値としてのbyte buffer全体の境界は`TODO: aot-byte-buffer-value` / `TODO: aot-buffer`として別管理する | `plugin-system-url`、`native-system-url-and-base64`、`native-system-table-byte-row-slice-alias`、`TODO: aot-byte-buffer-value`、`TODO: aot-buffer` |
| `拡張子抽出` / `拡張子変更` | 公式実測では`拡張子抽出("/a/.b/c.c++")`が`.c++`、`拡張子抽出(".bashrc")`が`.bashrc`、`拡張子変更("/a/.b/c.txt"," docx ")`が`/a/.b/c.docx`になる。拡張子として認識するのは末尾の`.`とASCII英数字・`_`・`-`・`+`だけで、フォルダ区切りは現在のホストの1種類だけを見る | Interpreterと純LLVM AOTで同じ文字集合・ECMAScript trim・現在OSの区切り文字を再現する。QuickJSは対象外。TODO: なし | `plugin-system-url`、`native-system-path-commands` |
| `終端パス追加` | 公式実測では`a/b`へ現在ホストの`path.sep`を1文字だけ追加し、POSIXでは`a/b/`、Windowsでは`a/b\`になる。既に末尾区切り文字があれば追加しない | Interpreterと純LLVM AOTで現在OSの区切り文字を1文字だけ追加する。QuickJSは対象外。TODO: なし | `plugin-system-url`、`native-system-path-commands` |
| `終端パス除去` / `終端パス削除` | 公式実測では末尾の現在ホスト区切り文字を全削除せず1文字だけ除去する。POSIXの`a/b//`は`a/b/`、Windowsでは入力末尾が`/`のため`a/b//`のままになり、別名も同じ結果になる | Interpreterと純LLVM AOTで同じ1文字除去と別名登録を行う。QuickJSは対象外。TODO: なし | `plugin-system-url`、`native-system-path-commands` |

## Nodeホスト・ファイル・圧縮

| 命令 | 公式v3.7.24の実際の挙動 | lnakoの扱い | 差分テストID |
|---|---|---|---|
| `OS取得` / `OSアーキテクチャ取得` | Nodeの`process.platform`はmacOS/Linux/Windowsで`darwin`/`linux`/`win32`、`os.arch()`はarm64/x64/ia32を返す | Interpreterと純LLVM AOTはNode互換のOS・CPU名を使う。AOTは実行時ホストではなく、生成物のコンパイル対象を`builtin`から決定する | `plugin-node-path-host`、`native-node-os-identity` |
| `コマンドライン` / `ナデシコランタイム` / `ナデシコランタイムパス` | 公式は`nodeProcess.argv`全体を`コマンドライン`へ入れ、`argv[0]`を`ナデシコランタイムパス`、その`path.basename`を`ナデシコランタイム`へ入れる。したがって公式CLIではNode実行ファイルとcnako3スクリプトも配列に含まれる | InterpreterはホストContextのargvをそのまま値化する。純LLVM AOTは生成`main(i32 argc, ptr argv)`から`argv[0..argc)`をUTF-16配列へ変換し、`argv[0]`とbasenameを同じ値へ設定する。JavaScript runtimeは使わない。Windowsの非ASCII argvをNodeのWTF-8文字列と完全一致させる境界は`TODO: aot-node-windows-wtf8-argv`として残す | `plugin-node-path-host`、`native-node-command-line-constants`、Interpreter/AOT |
| `終` / `終了` / `プロセス終` | `終`と`終了`はNodeの`process.exit(0)`相当で直ちに終了し、`プロセス終`は引数をNumberへ変換して終了コードを8bitへ正規化する。公式CLI・生成JavaScriptとも、終了前に出力した`BEFORE`だけが残り、後続の`AFTER`は実行されない | Interpreterと純LLVM AOTで同じ3命令を処理する。AOTは終了前にC標準出力とdispatch traceをflushし、有限値を`0..255`へ剰余正規化し、非有限値を0にする。対象経路はInterpreter・AOTで、QuickJS互換モードはNode host命令の証拠対象外。未実装ではなく、TODOはない | `plugin-node-exit-alias`、`plugin-node-exit-japanese-alias`、`plugin-node-exit-code`、`native-node-exit-alias`、`native-node-exit-japanese-alias`、`native-node-exit-code` |
| `強制終了時` | 登録した関数へ未定義値を渡し、関数がtruthyを返したときはSIGINT後に終了コード0で終了する。falseyを返した場合は終了せず、後続の非同期処理を続ける | InterpreterはNode hostのSIGINT flagをイベントループの20ms sliceごとに消費する。純LLVM AOTはJS runtimeを組み込まず、POSIXのSIGINT／Windowsのコンソール制御イベントをatomic flagへ取り込み、待機sliceごとに同じ順序でcallbackを呼ぶ。7経路の登録とfalse継続は`native-node-interrupt-registration`、POSIXの実割り込みは`plugin-node-interrupt`で比較する。Windows外部コンソール制御イベントの自動発生は未実装境界 `TODO: node-interrupt-windows-console-test` として残す | `plugin-node-interrupt`、`native-node-interrupt-registration`、`TODO: node-interrupt-windows-console-test` |
| `存在` / `フォルダ存在` | Nodeの`fs.statSync(PATH)`が成功すれば`存在`はtrueを返し、`フォルダ存在`はstat結果がディレクトリのときだけtrueを返す。未存在、権限不足、壊れたリンクなどstat失敗はfalseになる | InterpreterはNode hostのstat抽象へ委譲し、AOTは`std.Io.Dir.statFile`の成功結果とdirectory種別を使う。stat失敗を実行時エラーへ変換せずfalseへ正規化する。対象経路はInterpreter/AOTで、QuickJS互換モードは対象外。TODOはない | `plugin-node-file-core`、`native-node-file-existence` |
| `開` / `読` / `バイナリ読` / `保存` | 公式は`開`・`読`でファイルをUTF-8のlossy文字列として読み、`バイナリ読`ではBufferとしてバイト列を保持する。`保存`は文字列をUTF-8で、Bufferをそのまま書き、戻り値は`undefined` | InterpreterはNode hostへ委譲し、純LLVM AOTは`std.Io.Dir.cwd().readFileAlloc` / `writeFile`を使い、1 GiBの読み取り上限、UTF-8 lossy文字列、GC管理の`byte_buffer`を再現する。通常モードへJavaScript runtimeは混入しない。QuickJS互換モードは対象外。未実装TODOはない | `plugin-node-file-core`、`native-node-file-io` |
| `ホームディレクトリ取得` / `デスクトップ` / `マイドキュメント` / `テンポラリフォルダ` | 公式は`os.homedir()`相当のホーム値を返し、デスクトップとドキュメントはホームへ`Desktop`/`Documents`を連結する。`os.tmpdir()`相当の一時フォルダはmacOS/Linuxでは`TMPDIR`、Windowsでは`TEMP`、次に`TMP`を使い、末尾の区切り文字を除く。暗黙参照では同じ値がグローバルへ公開される | Interpreterはhost Contextのホーム・一時フォルダ値と同じ連結・末尾区切り規則を使う。純LLVM AOTは`HOME`/`USERPROFILE`、`TMPDIR`/`TEMP`/`TMP`を専用ABIで読み、明示呼出しに加えて暗黙グローバルも生成mainで初期化する。QuickJS互換モードは対象外。今回の境界にTODOはない | `plugin-node-path-host`、`native-node-directory-values` |
| `母艦パス` / `母艦パス取得` | 公式は実行対象ソースのパスを実行時のCWD基準で絶対化し、その親ディレクトリを返す。直接参照の定数と関数は同じ値になる | Interpreterはhost Contextの`source_path`を絶対パスへ解決して親ディレクトリを返す。純LLVM AOTは生成mainがソースパスを専用ABIへ渡し、実行時CWD基準で同じ規則を適用して`母艦パス`と`母艦パス取得`へ共有する。QuickJS互換モードは対象外。TODOはない | `plugin-node-path-host`、`native-node-mother-path` |
| `環境変数取得` / `環境変数一覧取得` | Nodeの`process.env[KEY]`は環境に存在するキーの値を文字列で返し、未設定キーは`undefined`になる。`Object.keys(process.env)`相当の一覧は環境ブロックのキーと値を列挙する | InterpreterはホストContextの環境表を参照し、純LLVM AOTはJS runtimeを使わずOSの環境ブロックから取得・辞書化する。AOTの一覧辞書は実行環境の列挙順を保持し、値が必要なキーだけをproperty参照できる | `plugin-node-path-host`、`native-node-environment`、`native-node-environment-list` |
| `カレントディレクトリ取得` / `作業フォルダ取得` | Node hostの`cwd()`は現在の作業フォルダを絶対パスで返す。2つの命令は同じ値を返す | Interpreterはhostのrealpath結果を使い、純LLVM AOTはC ABIの`getcwd`から現在の作業フォルダをUTF-16文字列へ変換する。fixtureではsymlinkを含まない作業フォルダを比較し、realpathとの差異は`TODO: aot-cwd-realpath`として残す | `plugin-node-path-host`、`native-node-current-directory`、`TODO: aot-cwd-realpath` |
| `ファイル名抽出` / `パス抽出` | Nodeの`path.basename` / `path.dirname`は末尾区切りを除いて最後の構成要素を返し、空文字はbasenameが空・dirnameが`.`、ルートはbasenameが空・dirnameがルートになる。構成要素間の連続区切りはdirnameに1つ少ない形で残る | Interpreterと純LLVM AOTで同じ基本規則を処理する。Nodeのdrive-relative path、UNC固有規則、入力型エラー文は`TODO: node-path-win32-boundary`として残す。QuickJSは対象外 | `plugin-node-path-host`、`native-node-path-components`、`TODO: node-path-win32-boundary` |
| `絶対パス変換` / `相対パス展開` | Nodeの`path.resolve`と`path.join`で入力を正規化し、相対入力は実行時のCWDを基準に絶対パスへ変換する。`相対パス展開`は先に2引数をjoinしてからresolveする | InterpreterはNode hostへ委譲し、純LLVM AOTはCWDをC ABIで取得してターゲットOSの`std.fs.path.resolve`/`join`で同じ順序を再現する。QuickJSは対象外。非UTF-8ファイル名とNodeの詳細な型エラー文は`TODO: aot-node-path-error`として残す | `plugin-node-path-host`、`native-node-path-resolve`、`TODO: aot-node-path-error` |
| `カレントディレクトリ変更` / `作業フォルダ変更` | Node hostは相対pathを現在の作業フォルダ基準で処理し、成功時は`undefined`を返す。存在しないpathなどの失敗は例外になる | Interpreterはhostの`Context.chdir`へ委譲し、純LLVM AOTはUTF-16からUTF-8へ変換したpathをC ABIの`chdir`へ渡し、成功時に`undefined`を返す。非ASCII Windows pathと失敗時の公式エラー文は未正規化のため`TODO: aot-cwd-change-unicode-error`として残す | `plugin-node-path-host`、`native-node-current-directory`、`TODO: aot-cwd-change-unicode-error` |
| `一時フォルダ作成` | 引数を親フォルダとして扱わず、`fs.mkdtempSync`の接頭辞へそのまま渡す。例えば`work-`ならカレントディレクトリ直下の`work-XXXXXX`を作る。空なら`os.tmpdir()`自体を接頭辞にするため、通常は一時ディレクトリの「中」ではなく同じ親に作る | Interpreterはhostの`fs.mkdtempSync`相当へ委譲する。純LLVM AOTはJS runtimeを使わず、同じ6文字の英数字suffixを付けてCWD基準（絶対prefixは絶対path）で新規ディレクトリを作り、衝突時は再試行する。QuickJS互換モードは対象外。TODOはない | `plugin-node-temporary-directory`、`native-node-temporary-directory` |
| `ブラウザ起動` / `エクスプローラー起動` | `ブラウザ起動`はOSのランチャーを非同期起動して直ちに戻る。`エクスプローラー起動`はWindowsでは`explorer`を起動して戻るが、macOS/Linuxでは`open`/`xdg-open`を起動した後も公式実装が`対応していないOSです`を投げる | Interpreterと純LLVM AOTはOS別ランチャーへ接続する。AOTは専用`node-process` ABIで実アプリを起動せず、`LNAKO_TEST_OPEN_EXTERNAL` hookによりURL・reveal経路とOS別の成功/エラー結果を比較する。通常モードへJS runtimeは混入せず、QuickJS互換モードは対象外。未実装TODOはない | `plugin-node-host-open-external` |
| `ファイル情報取得` | Nodeの`fs.Stats`を返すため、`isFile`と`isDirectory`は真偽値ではなくメソッド。メソッドだけを変数へ取り出して呼ぶと`this`が失われ、Node内部の`_checkModeProperty`参照で失敗する | InterpreterはNode hostのstat抽象へ委譲し、純LLVM AOTは`std.Io.Dir.statFile`から数値フィールドと名前付きメソッド関数値をGC管理の辞書へ生成する。なでしこ構文ではレシーバー付きメソッド呼び出しを直接表せないため、型・フィールド構成とAOTメソッド値の単体実行を差分検証する。QuickJSは対象外。意図的な公開形状であり未実装TODO: なし | `plugin-node-file-core`、`native-node-file-info` |
| `ファイル列挙`（順序） | `fs.readdirSync`が返す順序をそのまま使う。順序はAPI契約上保証されず、ファイルシステムにも依存する | InterpreterとAOTは再現可能性のため名前の昇順へ固定する。各OSでは同じfixtureを公式処理系でも実測する | `plugin-node-file-core`、`native-node-file-operations` |
| `ファイル列挙`（パターン） | `*`だけのglobではない。`.`と`*`だけを正規表現用に変換し、残りの`[]()+?`などは正規表現として解釈する。また先頭を固定しないため、`a*.txt`は`za.txt`にも一致する | InterpreterとAOTは同じ正規表現生成規則、末尾固定、大文字小文字無視を使う。AOTは純Zig regexpで処理する | `plugin-node-file-pattern-regexp`、`native-node-file-operations` |
| `全ファイル列挙` | 非再帰版と異なり、結果は絶対パス。ディレクトリ自身は含めず、該当ファイルだけを返す | InterpreterとAOTは再帰walkerで同じ形を返し、AOTはディレクトリを除外する | `plugin-node-file-core`、`native-node-file-operations` |
| `ファイルコピーデフォルト動作` | `上書き`、`上書`、`overwrite`のいずれかと完全一致すると、通常の`ファイルコピー`と`ファイル移動`も上書きを許可する。それ以外はコピー先が存在するだけで処理前に失敗する | Interpreterと純LLVM AOTは同じ3値を参照する。専用`node-file-operation` ABIは可変グローバルを受け取り、上書き命令は常に上書きする。`native-node-file-copy-default`は状態を、`native-node-file-operations`は実操作を検証する | `plugin-node-file-core`、`native-node-file-copy-default`、`native-node-file-operations` |
| `フォルダ作成` / `ファイルコピー` / `ファイル上書コピー` / `ファイル移動` / `ファイル上書移動` / `ファイル削除` | 公式は`mkdirpSync`、fs-extraのcopy/move/remove相当を使い、親ディレクトリを作成しながら再帰ディレクトリを処理する。通常操作は宛先存在時に失敗し、上書き操作は許可し、削除対象がなくても成功する。戻り値はいずれも`undefined` | InterpreterはNode hostへ委譲し、純LLVM AOTはJS runtimeを使わず`std.Io.Dir`の`make_path`、ファイルコピー、再帰walker、`deleteTree`へ接続する。QuickJSは対象外。callback・progress・cancelの境界は次の2行で別に記録する | `plugin-node-file-core`、`native-node-file-operations` |
| `コンソールクリア` | 公式は`console.clear()`を呼び、標準出力がTTYのときだけANSI制御コードを書き込む。比較ハーネスのpipe出力では表示内容を変更しない | InterpreterはNode host境界へ委譲し、純LLVM AOTはTTY依存の外部制御コードを通常出力へ混入させずno-opとする。QuickJSは対象外。TODOはない | `plugin-node-process`、`native-node-console-clear` |
| `ファイルサイズ取得` | 公式Node hostは`fs.statSync(PATH).size`を数値で返し、ファイルだけでなくディレクトリのstatサイズも対象にする。stat失敗時は例外になる | InterpreterはNode hostのstat抽象へ委譲し、純LLVM AOTは`std.Io.Dir.statFile`で同じpathを取得して`size`をbinary64へ変換する。QuickJSは対象外。TODO: なし | `plugin-node-file-core`、`native-node-file-size` |
| `文字コード変換サポート判定` | Node hostはエンコーディング名から区切り記号・大文字小文字・末尾の`:年`を正規化し、組み込みのUTF系・ASCII/Latin-1・Buffer用hex/base64・iconv-liteのlegacy/single-byte表に解決できればtrue、未知名ならfalseを返す | Interpreterと純LLVM AOTは同じ正規化と生成済み表を使い、Bufferを生成せずbooleanを返す。対象経路はInterpreter/AOTで、QuickJSは対象外。文字列・Bufferの変換本体は次行の8命令で別に記録する | `plugin-node-encoding`、`plugin-node-encoding-special`、`native-node-encoding-support` |
| `SJIS変換` / `SJIS取得` / `エンコーディング変換` / `エンコーディング取得` / `SJISファイル読` / `SJISファイル保存` / `EUCファイル読` / `EUCファイル保存` | 公式は生成済みiconv-lite表へ名前を解決する。変換命令はUTF-16文字列をSJISまたは指定encodingのBufferへ変換し、取得命令はBufferまたは数値配列を指定encodingからUTF-16文字列へ戻す。ファイル読は指定encodingでデコードした文字列、ファイル保存は指定encodingで書いた後に`undefined`を返す | Interpreterと純LLVM AOTは同じ純Zig codec・legacy表・UTF-16/byte-buffer境界を使う。AOTは`node-encoding`と`node-file-encoding` routeへ接続し、ファイル読取には1 GiB上限を適用する。`native-node-encoding`で公式CLI・公式生成JavaScript・`lnako run`・LLVM O0〜O3の代表的なSJIS/EUC-JP変換とファイルI/Oを比較する。QuickJSは対象外。代表fixtureの成功はcanonical execution evidenceの`verified`を意味せず、全命令の証拠状態は`evidence.json`の規則で別管理する | `plugin-node-encoding`、`plugin-node-encoding-special`、`native-node-encoding` |
| `標準入力全取得` / `尋` / `文字尋` / `標準入力取得時` | 公式は標準入力を一度読み、`標準入力全取得`では入力バイト列をUTF-8文字列として改行とCRを含めて返す。`尋`と`文字尋`は同じ入力を行へ分割し、行末直前のCRを除去する。`尋`は`Number(line)`がNaNでなければ数値、そうでなければ文字列を返し、`文字尋`は常に文字列を返す。`標準入力取得時`は登録した関数を同期的に実行し、各行をシステムグローバル`対象`へ設定してEOFまで渡す。EOFの末尾に改行のない部分行があればその行も返す | Interpreterは公式と同じstdinバッファを共有し、純LLVM AOTもJS runtimeを使わず`std.Io.File.stdin`から最大64 MiBを一度だけ読み、行命令とcallbackでは行末直前のCRだけを除去して`尋`の数値変換、`文字尋`の文字列境界、`標準入力取得時`の`対象`設定と同期callbackを再現する。`native-node-stdin-lines`、`native-node-stdin-callback`、`native-node-stdin-all`でプロンプト、CRLF、後続のraw全入力、全行callbackを公式CLI・生成JavaScript・Interpreter・LLVM O0〜O3と比較する | `plugin-node-stdin`、`plugin-node-stdin-callback`、`native-node-stdin-lines`、`native-node-stdin-callback`、`native-node-stdin-all` |
| `POSTデータ生成` | 公式は辞書を挿入順に走査し、キーと値を文字列化してURI componentのunreserved文字（英数字と`-_.!~*'()`）以外をUTF-8バイト単位で`%XX`へ変換し、`key=value&...`を返す。`{"a":"x y","日本":"語!"}`は`a=x%20y&%E6%97%A5%E6%9C%AC=%E8%AA%9E!`になる | Interpreterと純LLVM AOTは同じ辞書順、値の文字列化、UTF-8バイト単位の大文字16進エンコードを使う。HTTP serverの受信・応答は`plugin-httpserver-all`で実通信まで比較し、HTTPクライアント19 entryは純Zig request preparationへ接続してcallback・Promise・同期値の経路まで比較する | `plugin-node-http-post-data`、`native-node-http-post-data`、`plugin-node-http-callbacks`、`plugin-node-http-options-and-promises`、`plugin-node-http-async-values` |
| `AJAXオプション設定` | 公式は渡された値をそのまま`AJAXオプション`グローバルへ設定し、戻り値を返さない。辞書を設定した場合は挿入順を保ったまま後続の`AJAXオプション`参照で取得できる | InterpreterはNode pluginのglobal setterへ渡し、純LLVM AOTは専用`ajax-options` ABIで同じGC管理値を保持してHTTPクライアントの共通header・body設定へ適用する。Promise、callback、同期値を含むrequest routeを比較する | `plugin-node-http-options-and-promises`、`native-node-http-options-set` |
| `AJAX失敗時` | 公式は渡された関数値を`AJAX:ONERROR`グローバルへそのまま設定し、戻り値を返さない。後続のHTTP callbackが失敗した場合にこの関数をエラー文字列付きで呼び出す | InterpreterはNode pluginのglobal setterへ渡し、純LLVM AOTは専用`ajax-onerror` ABIで関数値を同じGC管理グローバルへ保持する。ネットワーク失敗はcallback経路では`AJAX:ONERROR`をイベントドレインから呼び、Response Promise経路ではPromiseを拒否する | `plugin-node-http-callbacks`、`plugin-node-http-discord-failure`、`plugin-node-http-onerror`、`native-node-http-onerror-set` |
| `圧縮解凍ツールパス` / `圧縮解凍ツールパス変更` | 公式Node hostは`7z`を既定値として公開し、変更命令に渡した値を次回の圧縮・解凍へ使う。変更命令自体は値を返さない | InterpreterはNode pluginの`archive_tool_path`とグローバルを更新する。純LLVM AOTは`7z`既定値と専用`archive-tool-path` ABIによる状態遷移を再現し、未変更時は内蔵stored-ZIP、明示変更後は指定7z互換ツールを公式と同じargvで使う。`native-node-archive-tool-path`と`plugin-node-native-archive`で状態・生成物を検証するが、公式7z外部経路との全OS差分証拠は未取得である | `native-node-archive-tool-path`、`plugin-node-native-archive`、`TODO: aot-node-archive-official-tool-diff` |
| Bufferの暗黙文字列化 | `Buffer.toString()`としてUTF-8復号する。不正列は例外にせずWHATWG UTF-8 decoderの最大部分列単位で`U+FFFD`へ置換する | 同じ。不正な各バイトを機械的に1文字ずつ置換せず、途中で切れた有効な接頭辞は1個の`U+FFFD`にする | `plugin-node-encoding` |
| BufferのJSON変換 | `JSON.stringify(Buffer)`は配列でなく`{"type":"Buffer","data":[...]}`を返す。整形指定時は通常オブジェクトと配列の両方の深さで字下げする | 同じオブジェクト形状と字下げにする | `plugin-node-encoding` |
| `ハッシュ値計算` / `ランダム配列生成`のバイト列 | エンコーディングを省略したハッシュ値は`Buffer`だが、乱数は`Uint8Array`。前者のJSONは`{"type":"Buffer","data":[...]}`、後者は`{"0":値,...}`となる。暗黙文字列化も前者はUTF-8復号、後者は10進値のカンマ区切り。両方ともTypedArray系の`byteLength`・`byteOffset`・`BYTES_PER_ELEMENT`とprototype propertyを持ち、`.buffer`は共有元の`ArrayBuffer`を返す。`ArrayBuffer`自身に`.buffer`はない | InterpreterはNode pluginのBuffer/Uint8Arrayを保持し、純LLVM AOTも`byte_buffer`タグの`buffer`/`uint8_array`/`array_buffer`種別をGC管理する。今回の`native-node-crypto`は添字・反復・JSON・文字列化とUUID形式を公式CLI・生成JavaScript・lnako run・AOT O0〜O3で比較し、`native-system-byte-buffer-property`でscalar propertyと代表的prototype methodの読み出し、`native-system-byte-buffer-backing`で`.buffer`の共有ArrayBuffer view、`byteLength`・`byteOffset`・constructor.name、ArrayBuffer自身の`.buffer`が`undefined`となる境界を比較する。`native-system-dictionary-byte-buffer-properties`でBuffer/Uint8Arrayのown index・`length`と、Buffer/Uint8Array/ArrayBufferのbyte length系scalar・代表的prototype propertyの存在判定を比較する。通常モードへJS runtimeを混入させず、QuickJSは標準命令の対象外である。`表列挿入`のBuffer.slice共有は`native-system-table-byte-row-slice-alias`で検証済み。backing buffer identity、receiver付きmethod呼出し、バイト列のown property列挙・削除は未実装境界として`TODO: aot-byte-buffer-value` / `TODO: aot-buffer`に分離する | `native-node-crypto`、`native-system-byte-buffer-property`、`native-system-byte-buffer-backing`、`native-system-dictionary-byte-buffer-properties`、`native-system-table-byte-row-slice-alias`、`plugin-node-crypto`、`TODO: aot-byte-buffer-value` |
| `ランダム配列生成`の個数 | `NaN`は0件、小数は0方向に切り捨てる。負数は`RangeError`、65,536件超は`crypto.getRandomValues`の`QuotaExceededError`になる | 0〜65,536件を許可し、同じ変換と拒否境界にする | `plugin-node-crypto` |
| 暗号命令の実行時例外と公式CLI終了コード | 公式`cnako3`は乱数個数の`RangeError`などをエラー表示しても、呼び出し経路によってプロセス終了コードが0のままになる | `lnako`は実行時エラーを非0終了にする。差分テストでは、公式側だけはエラー表示も失敗分類として扱う | `plugin-node-crypto` |
| `ハッシュ関数一覧取得` | Node 24がリンクするOpenSSLの`crypto.getHashes()`をそのまま返すため、利用可能な別名と順序はNode/OpenSSLのビルドに依存し得る | Interpreterと純LLVM AOTは製品へNode/JSを組み込まず、Node 24/OpenSSLの正式fixtureで固定した52名称を同じ順序の配列へ変換する。ハッシュ値計算を含む全名称・5出力形式のInterpreter検証と、一覧のAOT 7経路差分は分離して記録する | `plugin-node-crypto`、`native-node-hash-names` |
| `自分IPV6アドレス取得` | link-localアドレスも返すが、OSのscope名（`%en0`など）は戻り値に含めない | Interpreterと純LLVM AOTはOS APIから得たscopeを除去し、公式と同じIPv6文字列の配列を返す。AOTはPOSIXの`getifaddrs`またはWindowsの`GetAdaptersAddresses`を直接使い、JavaScript runtimeへ依存しない。未実装TODO: なし | `plugin-node-network-addresses`、`native-node-network-addresses` |
| `自分IPアドレス取得` / `自分IPV6アドレス取得`の対象 | Nodeの`os.networkInterfaces()`はlibuv経由でUPかつRUNNINGのインターフェイスだけを列挙する。Linuxで停止中のDocker bridgeにIPが残っていても結果に入らない | POSIXの`getifaddrs`結果をUP/RUNNINGフラグで絞り、単にアドレスがあるだけの停止インターフェイスは除外する。Windowsでは`GetAdaptersAddresses`の結果を公式のアドレス文字列へ変換する。未実装TODO: なし | `plugin-node-network-addresses`、`native-node-network-addresses` |
| 簡易HTTPサーバの未登録URL | callbackにも静的パスにも一致しないURLでは404を送らず、接続を開いたままにする。静的パスに一致した後でファイルがなければ404を返す | Interpreterと純LLVM AOTで、未登録URLの接続保留、静的パスの404、実通信の応答形式を再現する。`compare_http_server_oracle.mjs`はInterpreter、`compare_http_server_aot_oracle.mjs`はAOT O0〜O3を検証する | `plugin-httpserver-all` |
| HTTP callbackの文字列 | `AJAX送信時`、`POST送信時`、`AJAX失敗時`などは文字列から関数を名前解決せず、渡された値を後でそのまま呼ぶ。文字列を渡すと通信完了時に`TypeError`になる | callback値をそのまま保持する。名前付き関数は関数値または`～時には`構文で渡し、純LLVM AOTも同じ関数値をイベントドレインから呼ぶ | `plugin-node-http-callbacks`、`plugin-node-http-onerror` |
| `POSTフォーム送信時`のContent-Type | FormData本文にはboundaryがあるが、公式実装が手動設定する`Content-Type: multipart/form-data`にはboundary引数がない | この命令だけ欠落を再現し、`POSTフォーム送信`と`POSTフォーム保障送信`は正しいboundaryを付ける。純LLVM AOTは固定boundaryの本文を生成し、命令ごとのheader差を保持する | `plugin-node-http-callbacks`、`plugin-node-http-options-and-promises` |
| Promise版HTTP応答 | `AJAX保障送信`などは本文でなく`Response`を返す。暗黙文字列化は`[object Response]`、JSON変換は`{}`で、本文は`AJAX内容取得`により非同期取得する | HTTP応答を専用オブジェクト種別として保持し、純LLVM AOTも同じ文字列化・JSON形状にする。`AJAX内容取得`はResponse markerから本文を取り出す | `plugin-node-http-options-and-promises` |
| `AJAXバイナリ取得` | Nodeの`Buffer`や`Uint8Array`でなく`ArrayBuffer`を返す。暗黙文字列化は`[object ArrayBuffer]`、JSON変換は`{}` | バイト内容を保持した専用`ArrayBuffer`種別を返し、純LLVM AOTの`byte_buffer`から同じ形状を生成する | `plugin-node-http-async-values` |
| HTTPの`asyncFn`命令 | `POST送信`、`POSTフォーム送信`、`AJAXテキスト取得`、`AJAX_JSON取得`、`AJAXバイナリ取得`は、なでしこ言語側ではPromiseでなくawait済みの値になる | 呼び出しを完了まで待ち、純LLVM AOTも本文・JSON・ArrayBufferの値を直接返す | `plugin-node-http-async-values` |
| Discord送信の戻り値 | `DISCORD送信`と`DISCORDファイル送信`は`asyncFn`だが`return_none`でもあり、文として完了を待つ。戻り値を変数へ代入しようとすると公式コンパイラが拒否する | 文として同期的に完了を待ち、成功時は`undefined`とする。純LLVM AOTは2xx以外を失敗として扱う | `plugin-node-http-discord`、`plugin-node-http-discord-file`、`plugin-node-http-discord-failure` |
| LINE Notify命令 | 公式生成JavaScriptは`LINE送信`と`LINE画像送信`でAPIを呼ばず、2025年4月で利用不能になった旨の例外を常に投げる。公式CLIの直接実行は同じ失敗をstdoutへ表示して終了コード0、生成JavaScriptは実行時エラーで終了コード1となる | InterpreterとAOTは外部通信せず同じ廃止エラーを常に発生させる。QuickJS互換モードはこの標準命令の証拠経路に含めない。公式2経路の差があるため、native fixtureは公式生成JavaScriptをoracleにする。これはAPI廃止に対する意図的な制限であり、未実装ではない | `plugin-node-http-line-message-discontinued`、`plugin-node-http-line-image-discontinued`、`native-node-line-message-discontinued`、`native-node-line-image-discontinued` |
| HTTP callbackの完了順 | 複数のfetchは開始順ではなく実際の通信完了順にcallbackを呼ぶ | ホストワーカーの完了順を採番してメインイベントループでcallbackを実行する。純LLVM AOTも完了結果をclient task queueからイベントドレインへ渡す | `plugin-node-http-callbacks` |
| `AJAX受信`の対象更新 | `AJAX受信`はレスポンスのstatusを確認し、2xxのときだけ`対象`へ本文を設定する。失敗statusでは対象を更新しない | Interpreterと純LLVM AOTで同じ2xx境界を使い、AOTは完了taskをイベントドレインで処理する | `plugin-node-http-callbacks` |
| 簡易HTTPサーバの本文上限 | POST本文が10MiBを超えると413と`Request entity too large.`を返す。静的ファイルやGETにはこの上限を適用しない | Interpreterと純LLVM AOTは10MiB超をランタイムへ渡さず、入力を排出してから同じ413応答を返す | `plugin-httpserver-all` |
| `エンコーディング変換`（`hex`） | Nodeの`Buffer.from(S, "hex")`規則に従い、奇数桁の末尾を無視し、最初の非16進文字で変換を打ち切る | 同じ。例えば`4142zz`は`[65,66]`になる | `plugin-node-encoding` |
| `エンコーディング変換`（`base64`） | `iconv-lite`のencoderはUTF-16文字数を4文字境界で本体と端数に分け、それぞれを復号する。このため`QUI=xx`は`[65,66,199]`になる | 同じ分割規則と、空白・非base64文字・URL-safe文字の扱いを実装する | `plugin-node-encoding` |
| `cesu8` | 補助平面文字を通常のUTF-8の4バイト列ではなく、UTF-16のサロゲート各1単位を3バイトずつ、合計6バイトへ変換する。一方、取得時は連続するサロゲートをJS文字列として保持するため表示時には元の1文字へ戻る | UTF-16コード単位ごとのCESU-8変換と、孤立・不正列の置換規則を独立実装する | `plugin-node-encoding-special` |
| `utf7`の`+` | 非direct文字が`+`一文字だけの連続部分なら`+-`になるが、`+&日本`のように他の非direct文字と連続すると`+`も含めて全体がbase64化される | `iconv-lite`と同じ連続部分単位で判断する | `plugin-node-encoding-special` |
| `utf7-imap`の`&` | ASCII範囲はdirect文字だが、`&`だけは`&-`へエスケープする。非direct部分は通常の`/`を`,`へ置き換えたModified Base64になる | 同じdirect範囲、終端、Modified Base64規則で変換する | `plugin-node-encoding-special` |
| `ファイル処理時` / `ファイル処理強制停止` | 進捗の`件数`は再帰走査したファイル数で、ディレクトリ数を含めない。コールバック中に停止すると現在のファイルまではコピー済みになり、移動元は削除しない。未処理ファイルの親ディレクトリも作成しない。次のコピー・移動開始時には停止状態を解除する | Interpreterと純LLVM AOTは同じファイル単位・進捗辞書（件数/現在）・停止境界を使う。AOTは専用`node-file-callback` ABIで、同期進捗callbackはメイン実行中に、非同期callbackはworker完了後のイベントドレインで実行する。QuickJS互換モードは対象外。未実装TODOはない | `plugin-node-file-callbacks` |
| `起動待機` / `コマンド実行待機` / `起動` / `コマンド実行` / `起動時` | 待機命令はshell子プロセスの終了を待ち、`起動`・`コマンド実行`・`起動時`は完了を待たずに後続文へ進む一方、子プロセスはイベントループを生存させる。完了出力とcallbackは後続文やタイマーの実行中にも処理され、開始順ではなく完了順になる | Interpreterと純LLVM AOTは、同期命令を現在の作業フォルダで実行し、非同期命令をホストのワーカースレッドで実行する。完了順序を採番してメインのイベントループへ戻し、ランタイム値とcallbackをワーカースレッドから直接操作しない。AOTは専用`node-process` ABIを使い、QuickJS互換モードは対象外。未実装TODOはない | `plugin-node-process`、`plugin-node-process-order`、`plugin-node-process-completion-order` |
| `起動時`と短い`秒待`の競合 | `秒待`と子プロセスは別々の非同期処理なので、待ち時間内に子プロセスが終わる保証はない。同じ50ms待機でもマシン負荷により、後続の配列表示が`[]`または完了値入りになる | 実用上の完了順は公式と同じにする。差分fixtureでは十分な待機時間を確保し、50ms競合そのものを成功条件に使わない | `plugin-node-process` |
| `ファイルコピー時` / `ファイル移動時` / `ファイル削除時` / `圧縮時` / `解凍時` | 操作を開始すると直ちに後続文へ進み、完了後にコールバックを呼ぶ。後続文から出力ファイルを使う場合はコールバックで連鎖するか待機が必要 | Interpreterはhost workerで操作を開始し、完了とcallbackをメインのイベントループへ戻す。純LLVM AOTもファイルcallback variantsを専用`node-file-callback` ABIでworkerへ渡し、完了順にcallbackを実行する。`圧縮時`・`解凍時`はsource/destination/tool pathとcallbackをGC外のタスクへコピーして実行する。QuickJS互換モードは対象外。これらの経路に未実装TODOはない | `plugin-node-file-callbacks`、`plugin-node-native-archive` |
| `圧縮解凍ツールパス` / `圧縮解凍ツールパス変更` | 既定値`7z`を含め、常に指定外部コマンドを呼び出す | 未変更の既定値では内蔵stored-ZIP実装を使う。明示的に変更した後は指定した7-Zip互換コマンドを使う | `plugin-node-native-archive` |

既定ZIPを内蔵するのは、通常モードの実行ファイルを外部7-Zipへ依存させず、Windowsを含む3環境で同じ安全検査を行うためです。
外部ツールを選んだ場合の引数は公式と同じ`a -r` / `x -o... -y`形式です。

## 疎配列のコピー・連結境界

| 命令 | 公式v3.7.24の実際の挙動 | lnakoの扱い | 差分テストID / TODO |
|---|---|---|---|
| `参照` / `配列参照` / `配列足` / `配列範囲コピー` | 配列の`slice`（`参照`・`配列参照`）と、配列オペランドを展開する`concat`（`配列足`）は、配列長とown indexの欠損を保つ。穴を読み出した値は`undefined`だが、`slice`/`concat`結果の同じ位置はown propertyにならない。一方、`配列範囲コピー`は`slice`結果をJSON.stringify/parseするため、穴と明示的`undefined`のどちらも`null`のown要素になる。非配列の第2引数を`配列足`へ渡した場合は1要素として追加する | Interpreterと純LLVM AOTは値配列とpresence bitmapを同時に範囲コピー・連結し、`参照`/`配列参照`/配列同士の`配列足`では穴を維持する。`配列範囲コピー`は既存のJSON境界で穴を`null`へmaterializeする。これは意図的制限ではなく実装済みの公式互換で、QuickJS互換モードは標準命令の証拠経路に含めない。継承propertyと表命令内の疎配列は未実装境界として別TODOに残す | `native-system-array-sparse-copy-reference-concat`、`native-system-array-copy-reference-commands`、`TODO: reference-inherited-properties`、`TODO: sparse-array-presence` |

## 表命令のmap/filterと最上位疎配列

| 命令 | 公式v3.7.24の実際の挙動 | lnakoの扱い | 差分テストID / TODO |
|---|---|---|---|
| `表列取得` | `Array.map`相当なので、最上位配列のholeではcallbackを呼ばず、戻り値の同じ位置もholeになる。実在する行の内部propertyが欠損している場合は`undefined`を明示的な要素として返す。実在する`undefined`/`null`行はproperty参照時に例外になる | Interpreterと純LLVM AOTでsourceのpresenceを確認してholeを結果へ伝播し、実在行の欠損列は明示的`undefined`として追加する。own propertyを優先した標準prototype propertyの読み出しも行う。QuickJS互換モードは対象外。合成methodのreceiver付き呼出し、descriptor・identity・custom prototypeは`TODO: table-inherited-properties`、他の表命令の疎配列経路は`TODO: sparse-array-presence`として分離する | `native-system-table-sparse-map-filter`、`native-system-table-column-null-row-error`、`native-system-table-inherited-properties`、`TODO: table-inherited-properties`、`TODO: sparse-array-presence` |
| `表ピックアップ` / `表完全一致ピックアップ` | `Array.filter`相当なので、最上位配列のholeはcallbackを呼ばず結果へ入らない。実在する行だけを判定し、結果はdense配列になる。実在する`undefined`/`null`行はproperty参照時に例外になる | Interpreterと純LLVM AOTでholeをスキップし、条件に一致した実在行だけをdenseな結果へ追加する。実在するnullish行は既存の`TableRowMissing`経路で失敗させる。own propertyを優先した標準prototype propertyの読み出しも行う。QuickJS互換モードは対象外。合成methodのreceiver付き呼出し、descriptor・identity・custom prototypeは`TODO: table-inherited-properties`、他の表命令の疎配列経路は`TODO: sparse-array-presence`として分離する | `native-system-table-sparse-map-filter`、`native-system-table-byte-row-properties`、`native-system-table-inherited-properties`、`TODO: table-inherited-properties`、`TODO: sparse-array-presence` |

## 表命令のforEachと行slice

| 命令 | 公式v3.7.24の実際の挙動 | lnakoの扱い | 差分テストID / TODO |
|---|---|---|---|
| `表列挿入` / `表列削除` / `表列合計` | 外側の`forEach`は最上位配列のholeをスキップする。配列行の`slice`・`splice`は行内部のholeを保持し、列挿入後も欠損位置をown propertyにしない。`表列合計`は実在行だけを加算する | Interpreterと純LLVM AOTで外側のholeをスキップし、配列行のprefix/suffix・削除結果を値とpresence bitmapごと生成する。列挿入値は`undefined`読込後の代入なので明示要素になる。Bufferの列挿入sliceは共有storage view、Uint8Array・ArrayBufferはcopyとして扱う。QuickJS互換モードは対象外。他の表命令の疎配列経路は`TODO: sparse-array-presence`、byte buffer全体のprototype・view境界は`TODO: aot-byte-buffer-value`として分離する | `native-system-table-sparse-foreach-slice`、`native-system-table-byte-row-slice`、`native-system-table-byte-row-slice-alias`、`TODO: sparse-array-presence`、`TODO: aot-byte-buffer-value` |

## 表命令の関数name/lengthと継承prototype property

| 命令 | 公式v3.7.24の実際の挙動 | lnakoの扱い | 差分テストID / TODO |
|---|---|---|---|
| `表列取得`などの関数行property | 公式生成の匿名なでしこ関数は`Function.name`が空文字列で、rest引数wrapperのため`Function.length`が0になる。表命令はこのpropertyをそのまま読み出す | Interpreterと純LLVM AOTはlowering時の内部`__lambda$...`名を公開せず、`name`を空文字列へ変換し、`length`を0として返す。さらにown propertyを優先して標準prototypeのconstructor・代表的method・`__proto__`を読み出し、ユーザー関数のprototype objectも辞書として扱う。標準methodとbyte bufferの代表的methodは名前を持つ合成関数値であり、receiver付き呼出し、descriptor・identity・prototype変更、backing buffer identityはそれぞれ未実装境界として残す | `native-system-table-function-properties`、`native-system-table-inherited-properties`、`native-system-byte-buffer-property`、`TODO: table-inherited-properties`、`TODO: aot-byte-buffer-value` |
| `表列取得`などの継承property | 公式のrow property lookupはown propertyを優先した上で、Object/Array/String/Functionおよび数値・真偽値・BigIntのprototype chainを参照する。Buffer/Uint8ArrayはTypedArray/Buffer chain、ArrayBufferはArrayBuffer chainも参照し、constructor・代表的method・byteLength系scalar propertyを返す | Interpreterと純LLVM AOTは、表命令のrow property lookupで標準prototypeのfixture対象propertyを解決する。合成したmethod関数は公式の表示名を持つが、receiver付き呼出し、同一propertyのidentity、property descriptor、prototypeの変更、`__proto__`の完全なprototype object、RegExp・host objectの全property chainまでは再現しない。own propertyがある場合は継承値より優先する | `native-system-table-inherited-properties`、`native-system-byte-buffer-property`、`TODO: table-inherited-properties`、`TODO: aot-byte-buffer-value` |

## 更新規則

- 説明文と実装が食い違う場合は、固定した公式v3.7.24の実行結果を優先する。
- OS依存挙動は各正式OS上の公式処理系をオラクルにする。
- 互換性を維持するためだけに再現した不自然な挙動は、この文書と回帰fixtureの両方へ残す。
- 公式版を更新するときは各項目を再計測し、挙動が変わった項目を移行記録へ残す。
